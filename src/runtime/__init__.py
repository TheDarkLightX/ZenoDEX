"""Runtime authority boundary.

This package owns the explicit, auditable choice of *which* runtime (Python or
Rust) computes the canonical result for each consensus surface. See
``authority.py`` and ``docs/runtime/RUST_AUTHORITY_PROMOTION_GATE.md``.
"""
