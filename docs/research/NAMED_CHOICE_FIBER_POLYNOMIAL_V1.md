# Named Choice Fiber Polynomial V1

Status: `BOUNDED_RESEARCH_ONLY`

Authority: `NONE`

The complete preserved packet is in
[`experiments/named_choice_fiber_polynomial_v1/`](../../experiments/named_choice_fiber_polynomial_v1/README.md).

It formalizes a correlation-safe version of finite `+/-` branching as a named
multilinear function over a closed Boolean cube. The degree-one fragment gives
linear-time exact extrema. Multiplication requires explicit interaction fibers
and can grow exponentially.

The packet includes:

- an immutable Python reference object;
- fifteen bounded Tau queries against a pinned alpha toolchain;
- an independent deterministic report;
- semantic mutation checks;
- a source manifest and packet receipt;
- explicit mathematical corrections, prior-art boundaries, and nonclaims.

The strongest current use is research generation for adversarial scenario
families, Alignment-Theorem robustness, and ZRPF exact branch coverage. The
object is not part of settlement or production authority.
