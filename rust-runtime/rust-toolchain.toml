[toolchain]
# Exact release pin: release evidence must not move when the floating `stable`
# channel advances. Keep this synchronized with the Rust FCIS inventory and
# refresh the full runtime-shadow/CBC evidence before changing it.
channel = "1.87.0"
profile = "minimal"
components = ["rustfmt", "clippy"]
