"""
Auto-generated Python reference model for: zusd_generic_token_admission_v1
IR hash: sha256:afa6cd228a6e4a181f4de1281fbba7ee27aa72c2a66291425349fef6e547555e

Generated by ESSO (Evolutionary Spec Search Optimizer)

This file is standalone and has no ESSO dependencies.
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal, Mapping


@dataclass(frozen=True)
class State:
    """Model state."""
    canonical_supply_units: int
    violation_found: int


@dataclass(frozen=True)
class Command:
    """Command to execute."""
    tag: Literal('evaluate_burn', 'evaluate_mint', 'evaluate_transfer')
    args: Mapping[str, Any]


@dataclass(frozen=True)
class StepResult:
    """Result of a step execution."""
    ok: bool
    state: State | None = None
    effects: Mapping[str, Any] | None = None
    error: str | None = None


def init_state() -> State:
    """Return the initial state."""
    return State(
        canonical_supply_units=0,
        violation_found=0,
    )


def check_invariants(s: State) -> tuple[bool, str | None]:
    """Check all invariants. Returns (ok, first_failed_id)."""
    if not (isinstance(s.canonical_supply_units, int) and not isinstance(s.canonical_supply_units, bool) and 0 <= s.canonical_supply_units <= 4294967295):
        return False, 'domain_canonical_supply_units'
    if not (isinstance(s.violation_found, int) and not isinstance(s.violation_found, bool) and 0 <= s.violation_found <= 1):
        return False, 'domain_violation_found'
    if not ((0 == s.violation_found)):
        return False, 'inv_authority_and_recipient_partition_complete'
    return True, None


def step(s: State, cmd: Command) -> StepResult:
    """Execute a step. Returns StepResult."""
    # Check pre-state invariants
    ok, failed = check_invariants(s)
    if not ok:
        return StepResult(ok=False, error=f'pre-invariant violated: {failed}')

    if cmd.tag == 'evaluate_burn':
        if 'actor_is_monetary_authority' not in cmd.args or not (isinstance(cmd.args['actor_is_monetary_authority'], bool)):
            return StepResult(ok=False, error='invalid param actor_is_monetary_authority')
        if 'asset_is_canonical_zusd' not in cmd.args or not (isinstance(cmd.args['asset_is_canonical_zusd'], bool)):
            return StepResult(ok=False, error='invalid param asset_is_canonical_zusd')
        if 'recipient_is_reserved_protocol_address' not in cmd.args or not (isinstance(cmd.args['recipient_is_reserved_protocol_address'], bool)):
            return StepResult(ok=False, error='invalid param recipient_is_reserved_protocol_address')
        if not (True):
            return StepResult(ok=False, error='guard failed for evaluate_burn')
        # Compute updates (simultaneous)
        new_state = State(
            canonical_supply_units=s.canonical_supply_units,
            violation_found=(0 if ((0 == 0) and ((4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 2)) == (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 2))) and (((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) == ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd']))) and (s.canonical_supply_units == s.canonical_supply_units)) else 1),
        )
        effects = {
            'actor_is_monetary_authority': cmd.args['actor_is_monetary_authority'],
            'admitted': ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])),
            'asset_is_canonical_zusd': cmd.args['asset_is_canonical_zusd'],
            'canonical_zusd_supply_delta': 0,
            'decision_code': (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 2)),
            'exhaustive_case_ok': ((0 == 0) and ((4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 2)) == (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 2))) and (((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) == ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd']))) and (new_state.canonical_supply_units == new_state.canonical_supply_units)),
            'operation_code': 2,
            'post_canonical_supply_units': new_state.canonical_supply_units,
            'recipient_is_reserved_protocol_address': cmd.args['recipient_is_reserved_protocol_address'],
            'rejection_noop': ((new_state.canonical_supply_units == new_state.canonical_supply_units) or ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd']))),
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'evaluate_mint':
        if 'actor_is_monetary_authority' not in cmd.args or not (isinstance(cmd.args['actor_is_monetary_authority'], bool)):
            return StepResult(ok=False, error='invalid param actor_is_monetary_authority')
        if 'asset_is_canonical_zusd' not in cmd.args or not (isinstance(cmd.args['asset_is_canonical_zusd'], bool)):
            return StepResult(ok=False, error='invalid param asset_is_canonical_zusd')
        if 'recipient_is_reserved_protocol_address' not in cmd.args or not (isinstance(cmd.args['recipient_is_reserved_protocol_address'], bool)):
            return StepResult(ok=False, error='invalid param recipient_is_reserved_protocol_address')
        if not (True):
            return StepResult(ok=False, error='guard failed for evaluate_mint')
        # Compute updates (simultaneous)
        new_state = State(
            canonical_supply_units=s.canonical_supply_units,
            violation_found=(0 if ((0 == 0) and ((4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 1)) == (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 1))) and (((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) == ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd']))) and (s.canonical_supply_units == s.canonical_supply_units)) else 1),
        )
        effects = {
            'actor_is_monetary_authority': cmd.args['actor_is_monetary_authority'],
            'admitted': ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])),
            'asset_is_canonical_zusd': cmd.args['asset_is_canonical_zusd'],
            'canonical_zusd_supply_delta': 0,
            'decision_code': (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 1)),
            'exhaustive_case_ok': ((0 == 0) and ((4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 1)) == (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) else 1))) and (((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd'])) == ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd']))) and (new_state.canonical_supply_units == new_state.canonical_supply_units)),
            'operation_code': 1,
            'post_canonical_supply_units': new_state.canonical_supply_units,
            'recipient_is_reserved_protocol_address': cmd.args['recipient_is_reserved_protocol_address'],
            'rejection_noop': ((new_state.canonical_supply_units == new_state.canonical_supply_units) or ((not cmd.args['actor_is_monetary_authority']) and (not cmd.args['asset_is_canonical_zusd']))),
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'evaluate_transfer':
        if 'actor_is_monetary_authority' not in cmd.args or not (isinstance(cmd.args['actor_is_monetary_authority'], bool)):
            return StepResult(ok=False, error='invalid param actor_is_monetary_authority')
        if 'asset_is_canonical_zusd' not in cmd.args or not (isinstance(cmd.args['asset_is_canonical_zusd'], bool)):
            return StepResult(ok=False, error='invalid param asset_is_canonical_zusd')
        if 'recipient_is_reserved_protocol_address' not in cmd.args or not (isinstance(cmd.args['recipient_is_reserved_protocol_address'], bool)):
            return StepResult(ok=False, error='invalid param recipient_is_reserved_protocol_address')
        if not (True):
            return StepResult(ok=False, error='guard failed for evaluate_transfer')
        # Compute updates (simultaneous)
        new_state = State(
            canonical_supply_units=s.canonical_supply_units,
            violation_found=(0 if ((0 == 0) and ((4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) else 3)) == (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) else 3))) and (((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) == ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address'])))) and (s.canonical_supply_units == s.canonical_supply_units)) else 1),
        )
        effects = {
            'actor_is_monetary_authority': cmd.args['actor_is_monetary_authority'],
            'admitted': ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))),
            'asset_is_canonical_zusd': cmd.args['asset_is_canonical_zusd'],
            'canonical_zusd_supply_delta': 0,
            'decision_code': (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) else 3)),
            'exhaustive_case_ok': ((0 == 0) and ((4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) else 3)) == (4 if cmd.args['actor_is_monetary_authority'] else (0 if ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) else 3))) and (((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address']))) == ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address'])))) and (new_state.canonical_supply_units == new_state.canonical_supply_units)),
            'operation_code': 0,
            'post_canonical_supply_units': new_state.canonical_supply_units,
            'recipient_is_reserved_protocol_address': cmd.args['recipient_is_reserved_protocol_address'],
            'rejection_noop': ((new_state.canonical_supply_units == new_state.canonical_supply_units) or ((not cmd.args['actor_is_monetary_authority']) and ((not cmd.args['asset_is_canonical_zusd']) or (not cmd.args['recipient_is_reserved_protocol_address'])))),
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    else:
        return StepResult(ok=False, error=f'unknown action: {cmd.tag}')


# === Hypothesis test scaffolding ===
# Uncomment and install hypothesis to use property-based testing

# from hypothesis import given, strategies as st
# from hypothesis.stateful import RuleBasedStateMachine, rule, initialize
#
# class ModelStateMachine(RuleBasedStateMachine):
#     def __init__(self):
#         super().__init__()
#         self.state = init_state()
#
#     @initialize()
#     def init(self):
#         self.state = init_state()
#         ok, _ = check_invariants(self.state)
#         assert ok, 'init violates invariants'
#
#     # @rule(actor_is_monetary_authority=st.integers(), asset_is_canonical_zusd=st.integers(), recipient_is_reserved_protocol_address=st.integers())
#     # def evaluate_burn(self, actor_is_monetary_authority, asset_is_canonical_zusd, recipient_is_reserved_protocol_address):
#     #     cmd = Command(tag='evaluate_burn', args={'actor_is_monetary_authority': actor_is_monetary_authority, 'asset_is_canonical_zusd': asset_is_canonical_zusd, 'recipient_is_reserved_protocol_address': recipient_is_reserved_protocol_address})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(actor_is_monetary_authority=st.integers(), asset_is_canonical_zusd=st.integers(), recipient_is_reserved_protocol_address=st.integers())
#     # def evaluate_mint(self, actor_is_monetary_authority, asset_is_canonical_zusd, recipient_is_reserved_protocol_address):
#     #     cmd = Command(tag='evaluate_mint', args={'actor_is_monetary_authority': actor_is_monetary_authority, 'asset_is_canonical_zusd': asset_is_canonical_zusd, 'recipient_is_reserved_protocol_address': recipient_is_reserved_protocol_address})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(actor_is_monetary_authority=st.integers(), asset_is_canonical_zusd=st.integers(), recipient_is_reserved_protocol_address=st.integers())
#     # def evaluate_transfer(self, actor_is_monetary_authority, asset_is_canonical_zusd, recipient_is_reserved_protocol_address):
#     #     cmd = Command(tag='evaluate_transfer', args={'actor_is_monetary_authority': actor_is_monetary_authority, 'asset_is_canonical_zusd': asset_is_canonical_zusd, 'recipient_is_reserved_protocol_address': recipient_is_reserved_protocol_address})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
# TestModel = ModelStateMachine.TestCase


def replay_trace(commands: list[tuple[str, dict[str, Any]]]) -> list[StepResult]:
    """Replay a trace of commands and return results."""
    results: list[StepResult] = []
    s = init_state()
    for tag, args in commands:
        cmd = Command(tag=tag, args=args)
        result = step(s, cmd)
        results.append(result)
        if result.ok and result.state is not None:
            s = result.state
        else:
            break
    return results


if __name__ == '__main__':
    # Quick sanity check
    s0 = init_state()
    ok, failed = check_invariants(s0)
    print(f'Initial state: {s0}')
    print(f'Invariants OK: {ok}')
    if not ok:
        print(f'  Failed: {failed}')
