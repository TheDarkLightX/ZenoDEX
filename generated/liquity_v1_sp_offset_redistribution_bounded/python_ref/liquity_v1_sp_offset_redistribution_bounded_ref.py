"""
Auto-generated Python reference model for: liquity_v1_sp_offset_redistribution_bounded
IR hash: sha256:7b505df182da9fbff843b199366d6bcb6dd63736d573a9fe5e243f3d70db2fec

Generated by ESSO (Evolutionary Spec Search Optimizer)

This file is standalone and has no ESSO dependencies.
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal, Mapping


@dataclass(frozen=True)
class State:
    """Model state."""
    branch: int
    collateral: int
    collateral_to_redistribute: int
    collateral_to_sp: int
    debt: int
    debt_to_offset: int
    debt_to_redistribute: int
    sp_deposits: int


@dataclass(frozen=True)
class Command:
    """Command to execute."""
    tag: Literal('partition_offset_and_redistribution',)
    args: Mapping[str, Any]


@dataclass(frozen=True)
class StepResult:
    """Result of a step execution."""
    ok: bool
    state: State | None = None
    effects: Mapping[str, Any] | None = None
    error: str | None = None


def init_state() -> State:
    """Return the initial state."""
    return State(
        branch=1,
        collateral=3,
        collateral_to_redistribute=2,
        collateral_to_sp=1,
        debt=2,
        debt_to_offset=1,
        debt_to_redistribute=1,
        sp_deposits=1,
    )


def check_invariants(s: State) -> tuple[bool, str | None]:
    """Check all invariants. Returns (ok, first_failed_id)."""
    if not (isinstance(s.branch, int) and not isinstance(s.branch, bool) and 0 <= s.branch <= 2):
        return False, 'domain_branch'
    if not (isinstance(s.collateral, int) and not isinstance(s.collateral, bool) and 0 <= s.collateral <= 4):
        return False, 'domain_collateral'
    if not (isinstance(s.collateral_to_redistribute, int) and not isinstance(s.collateral_to_redistribute, bool) and 0 <= s.collateral_to_redistribute <= 4):
        return False, 'domain_collateral_to_redistribute'
    if not (isinstance(s.collateral_to_sp, int) and not isinstance(s.collateral_to_sp, bool) and 0 <= s.collateral_to_sp <= 4):
        return False, 'domain_collateral_to_sp'
    if not (isinstance(s.debt, int) and not isinstance(s.debt, bool) and 1 <= s.debt <= 4):
        return False, 'domain_debt'
    if not (isinstance(s.debt_to_offset, int) and not isinstance(s.debt_to_offset, bool) and 0 <= s.debt_to_offset <= 4):
        return False, 'domain_debt_to_offset'
    if not (isinstance(s.debt_to_redistribute, int) and not isinstance(s.debt_to_redistribute, bool) and 0 <= s.debt_to_redistribute <= 4):
        return False, 'domain_debt_to_redistribute'
    if not (isinstance(s.sp_deposits, int) and not isinstance(s.sp_deposits, bool) and 0 <= s.sp_deposits <= 4):
        return False, 'domain_sp_deposits'
    if not (((0 if (0 == s.sp_deposits) else (1 if (s.sp_deposits < s.debt) else 2)) == s.branch)):
        return False, 'inv_branch_exact'
    if not (((s.collateral_to_redistribute + s.collateral_to_sp) == s.collateral)):
        return False, 'inv_collateral_partition'
    if not (((0 if s.debt == 0 else (((s.collateral * s.debt_to_offset) // s.debt) + (1 if ((s.collateral * s.debt_to_offset) % s.debt) < 0 else 0))) == s.collateral_to_sp)):
        return False, 'inv_collateral_share_exact_floor'
    if not (((s.debt_to_offset + s.debt_to_redistribute) == s.debt)):
        return False, 'inv_debt_partition'
    if not ((min(s.debt, s.sp_deposits) == s.debt_to_offset)):
        return False, 'inv_offset_exact'
    return True, None


def step(s: State, cmd: Command) -> StepResult:
    """Execute a step. Returns StepResult."""
    # Check pre-state invariants
    ok, failed = check_invariants(s)
    if not ok:
        return StepResult(ok=False, error=f'pre-invariant violated: {failed}')

    if cmd.tag == 'partition_offset_and_redistribution':
        if 'new_debt' not in cmd.args or not (isinstance(cmd.args['new_debt'], int) and not isinstance(cmd.args['new_debt'], bool) and 1 <= cmd.args['new_debt'] <= 4):
            return StepResult(ok=False, error='invalid param new_debt')
        if 'new_collateral' not in cmd.args or not (isinstance(cmd.args['new_collateral'], int) and not isinstance(cmd.args['new_collateral'], bool) and 0 <= cmd.args['new_collateral'] <= 4):
            return StepResult(ok=False, error='invalid param new_collateral')
        if 'new_sp_deposits' not in cmd.args or not (isinstance(cmd.args['new_sp_deposits'], int) and not isinstance(cmd.args['new_sp_deposits'], bool) and 0 <= cmd.args['new_sp_deposits'] <= 4):
            return StepResult(ok=False, error='invalid param new_sp_deposits')
        if not (True):
            return StepResult(ok=False, error='guard failed for partition_offset_and_redistribution')
        # Compute updates (simultaneous)
        new_state = State(
            branch=(0 if (0 == cmd.args['new_sp_deposits']) else (1 if (cmd.args['new_sp_deposits'] < cmd.args['new_debt']) else 2)),
            collateral=cmd.args['new_collateral'],
            collateral_to_redistribute=(cmd.args['new_collateral'] - (0 if cmd.args['new_debt'] == 0 else (((min(cmd.args['new_debt'], cmd.args['new_sp_deposits']) * cmd.args['new_collateral']) // cmd.args['new_debt']) + (1 if ((min(cmd.args['new_debt'], cmd.args['new_sp_deposits']) * cmd.args['new_collateral']) % cmd.args['new_debt']) < 0 else 0)))),
            collateral_to_sp=(0 if cmd.args['new_debt'] == 0 else (((min(cmd.args['new_debt'], cmd.args['new_sp_deposits']) * cmd.args['new_collateral']) // cmd.args['new_debt']) + (1 if ((min(cmd.args['new_debt'], cmd.args['new_sp_deposits']) * cmd.args['new_collateral']) % cmd.args['new_debt']) < 0 else 0))),
            debt=cmd.args['new_debt'],
            debt_to_offset=min(cmd.args['new_debt'], cmd.args['new_sp_deposits']),
            debt_to_redistribute=(cmd.args['new_debt'] - min(cmd.args['new_debt'], cmd.args['new_sp_deposits'])),
            sp_deposits=cmd.args['new_sp_deposits'],
        )
        effects = {
            'branch': new_state.branch,
            'collateral_to_redistribute': new_state.collateral_to_redistribute,
            'collateral_to_sp': new_state.collateral_to_sp,
            'debt_to_offset': new_state.debt_to_offset,
            'debt_to_redistribute': new_state.debt_to_redistribute,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    else:
        return StepResult(ok=False, error=f'unknown action: {cmd.tag}')


# === Hypothesis test scaffolding ===
# Uncomment and install hypothesis to use property-based testing

# from hypothesis import given, strategies as st
# from hypothesis.stateful import RuleBasedStateMachine, rule, initialize
#
# class ModelStateMachine(RuleBasedStateMachine):
#     def __init__(self):
#         super().__init__()
#         self.state = init_state()
#
#     @initialize()
#     def init(self):
#         self.state = init_state()
#         ok, _ = check_invariants(self.state)
#         assert ok, 'init violates invariants'
#
#     # @rule(new_debt=st.integers(), new_collateral=st.integers(), new_sp_deposits=st.integers())
#     # def partition_offset_and_redistribution(self, new_debt, new_collateral, new_sp_deposits):
#     #     cmd = Command(tag='partition_offset_and_redistribution', args={'new_debt': new_debt, 'new_collateral': new_collateral, 'new_sp_deposits': new_sp_deposits})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
# TestModel = ModelStateMachine.TestCase


def replay_trace(commands: list[tuple[str, dict[str, Any]]]) -> list[StepResult]:
    """Replay a trace of commands and return results."""
    results: list[StepResult] = []
    s = init_state()
    for tag, args in commands:
        cmd = Command(tag=tag, args=args)
        result = step(s, cmd)
        results.append(result)
        if result.ok and result.state is not None:
            s = result.state
        else:
            break
    return results


if __name__ == '__main__':
    # Quick sanity check
    s0 = init_state()
    ok, failed = check_invariants(s0)
    print(f'Initial state: {s0}')
    print(f'Invariants OK: {ok}')
    if not ok:
        print(f'  Failed: {failed}')
