"""
Auto-generated Python reference model for: lp_ratio_calculator_v7
IR hash: sha256:33a238faeee9e52207e1114d98488f98e974881af6a825566fef2e0e59672b0c

Generated by an offline verifier toolchain.

This file is standalone and has no toolchain dependencies.
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal, Mapping


@dataclass(frozen=True)
class State:
    """Model state."""
    reserve0: int
    reserve1: int


@dataclass(frozen=True)
class Command:
    """Command to execute."""
    tag: Literal('calculate_optimal',)
    args: Mapping[str, Any]


@dataclass(frozen=True)
class StepResult:
    """Result of a step execution."""
    ok: bool
    state: State | None = None
    effects: Mapping[str, Any] | None = None
    error: str | None = None


def init_state() -> State:
    """Return the initial state."""
    return State(
        reserve0=0,
        reserve1=0,
    )


def check_invariants(s: State) -> tuple[bool, str | None]:
    """Check all invariants. Returns (ok, first_failed_id)."""
    if not (isinstance(s.reserve0, int) and not isinstance(s.reserve0, bool) and 0 <= s.reserve0 <= 3000000000):
        return False, 'domain_reserve0'
    if not (isinstance(s.reserve1, int) and not isinstance(s.reserve1, bool) and 0 <= s.reserve1 <= 3000000000):
        return False, 'domain_reserve1'
    if not ((((0 == s.reserve0) and (0 == s.reserve1)) or ((s.reserve0 > 0) and (s.reserve1 > 0)))):
        return False, 'inv_pool_consistency'
    if not (((s.reserve0 >= 0) and (s.reserve1 >= 0))):
        return False, 'inv_reserves_nonneg'
    return True, None


def step(s: State, cmd: Command) -> StepResult:
    """Execute a step. Returns StepResult."""
    # Check pre-state invariants
    ok, failed = check_invariants(s)
    if not ok:
        return StepResult(ok=False, error=f'pre-invariant violated: {failed}')

    if cmd.tag == 'calculate_optimal':
        if 'desired0' not in cmd.args or not (isinstance(cmd.args['desired0'], int) and not isinstance(cmd.args['desired0'], bool) and 1 <= cmd.args['desired0'] <= 1000000000):
            return StepResult(ok=False, error='invalid param desired0')
        if 'desired1' not in cmd.args or not (isinstance(cmd.args['desired1'], int) and not isinstance(cmd.args['desired1'], bool) and 1 <= cmd.args['desired1'] <= 1000000000):
            return StepResult(ok=False, error='invalid param desired1')
        if not (((cmd.args['desired0'] > 0) and (cmd.args['desired1'] > 0) and (((0 == s.reserve0) and (0 == s.reserve1)) or ((s.reserve0 > 0) and (s.reserve1 > 0))))):
            return StepResult(ok=False, error='guard failed for calculate_optimal')
        # Compute updates (simultaneous)
        new_state = State(
            reserve0=s.reserve0,
            reserve1=s.reserve1,
        )
        effects = {
            'is_initial': ((0 == new_state.reserve0) and (0 == new_state.reserve1)),
            'optimal0': (cmd.args['desired0'] if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else (cmd.args['desired0'] if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else (0 if new_state.reserve1 == 0 else (((cmd.args['desired1'] * new_state.reserve0) // new_state.reserve1) + (1 if ((cmd.args['desired1'] * new_state.reserve0) % new_state.reserve1) < 0 else 0))))),
            'optimal0_le_desired0': ((cmd.args['desired0'] if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else (cmd.args['desired0'] if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else (0 if new_state.reserve1 == 0 else (((cmd.args['desired1'] * new_state.reserve0) // new_state.reserve1) + (1 if ((cmd.args['desired1'] * new_state.reserve0) % new_state.reserve1) < 0 else 0))))) <= cmd.args['desired0']),
            'optimal1': (cmd.args['desired1'] if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else ((0 if new_state.reserve0 == 0 else (((cmd.args['desired0'] * new_state.reserve1) // new_state.reserve0) + (1 if ((cmd.args['desired0'] * new_state.reserve1) % new_state.reserve0) < 0 else 0))) if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else cmd.args['desired1'])),
            'optimal1_le_desired1': ((cmd.args['desired1'] if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else ((0 if new_state.reserve0 == 0 else (((cmd.args['desired0'] * new_state.reserve1) // new_state.reserve0) + (1 if ((cmd.args['desired0'] * new_state.reserve1) % new_state.reserve0) < 0 else 0))) if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else cmd.args['desired1'])) <= cmd.args['desired1']),
            'refund0': (0 if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else (0 if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else (cmd.args['desired0'] - (0 if new_state.reserve1 == 0 else (((cmd.args['desired1'] * new_state.reserve0) // new_state.reserve1) + (1 if ((cmd.args['desired1'] * new_state.reserve0) % new_state.reserve1) < 0 else 0)))))),
            'refund0_nonneg': ((0 if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else (0 if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else (cmd.args['desired0'] - (0 if new_state.reserve1 == 0 else (((cmd.args['desired1'] * new_state.reserve0) // new_state.reserve1) + (1 if ((cmd.args['desired1'] * new_state.reserve0) % new_state.reserve1) < 0 else 0)))))) >= 0),
            'refund1': (0 if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else ((cmd.args['desired1'] - (0 if new_state.reserve0 == 0 else (((cmd.args['desired0'] * new_state.reserve1) // new_state.reserve0) + (1 if ((cmd.args['desired0'] * new_state.reserve1) % new_state.reserve0) < 0 else 0)))) if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else 0)),
            'refund1_nonneg': ((0 if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else ((cmd.args['desired1'] - (0 if new_state.reserve0 == 0 else (((cmd.args['desired0'] * new_state.reserve1) // new_state.reserve0) + (1 if ((cmd.args['desired0'] * new_state.reserve1) % new_state.reserve0) < 0 else 0)))) if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else 0)) >= 0),
            'sum0_ok': (((0 if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else (0 if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else (cmd.args['desired0'] - (0 if new_state.reserve1 == 0 else (((cmd.args['desired1'] * new_state.reserve0) // new_state.reserve1) + (1 if ((cmd.args['desired1'] * new_state.reserve0) % new_state.reserve1) < 0 else 0)))))) + (cmd.args['desired0'] if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else (cmd.args['desired0'] if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else (0 if new_state.reserve1 == 0 else (((cmd.args['desired1'] * new_state.reserve0) // new_state.reserve1) + (1 if ((cmd.args['desired1'] * new_state.reserve0) % new_state.reserve1) < 0 else 0)))))) == cmd.args['desired0']),
            'sum1_ok': (((0 if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else ((cmd.args['desired1'] - (0 if new_state.reserve0 == 0 else (((cmd.args['desired0'] * new_state.reserve1) // new_state.reserve0) + (1 if ((cmd.args['desired0'] * new_state.reserve1) % new_state.reserve0) < 0 else 0)))) if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else 0)) + (cmd.args['desired1'] if ((0 == new_state.reserve0) and (0 == new_state.reserve1)) else ((0 if new_state.reserve0 == 0 else (((cmd.args['desired0'] * new_state.reserve1) // new_state.reserve0) + (1 if ((cmd.args['desired0'] * new_state.reserve1) % new_state.reserve0) < 0 else 0))) if ((cmd.args['desired0'] * new_state.reserve1) <= (cmd.args['desired1'] * new_state.reserve0)) else cmd.args['desired1']))) == cmd.args['desired1']),
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    else:
        return StepResult(ok=False, error=f'unknown action: {cmd.tag}')


# === Hypothesis test scaffolding ===
# Uncomment and install hypothesis to use property-based testing

# from hypothesis import given, strategies as st
# from hypothesis.stateful import RuleBasedStateMachine, rule, initialize
#
# class ModelStateMachine(RuleBasedStateMachine):
#     def __init__(self):
#         super().__init__()
#         self.state = init_state()
#
#     @initialize()
#     def init(self):
#         self.state = init_state()
#         ok, _ = check_invariants(self.state)
#         assert ok, 'init violates invariants'
#
#     # @rule(desired0=st.integers(), desired1=st.integers())
#     # def calculate_optimal(self, desired0, desired1):
#     #     cmd = Command(tag='calculate_optimal', args={'desired0': desired0, 'desired1': desired1})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
# TestModel = ModelStateMachine.TestCase


def replay_trace(commands: list[tuple[str, dict[str, Any]]]) -> list[StepResult]:
    """Replay a trace of commands and return results."""
    results: list[StepResult] = []
    s = init_state()
    for tag, args in commands:
        cmd = Command(tag=tag, args=args)
        result = step(s, cmd)
        results.append(result)
        if result.ok and result.state is not None:
            s = result.state
        else:
            break
    return results


if __name__ == '__main__':
    # Quick sanity check
    s0 = init_state()
    ok, failed = check_invariants(s0)
    print(f'Initial state: {s0}')
    print(f'Invariants OK: {ok}')
    if not ok:
        print(f'  Failed: {failed}')
