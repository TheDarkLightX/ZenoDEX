"""
Auto-generated Python reference model for: cpmm_swap_v7
IR hash: sha256:3377821fc75b6df723326495f9cc4b60b0baa17b72ac23a5ef4ae393968c3809

Generated by an offline verifier toolchain.

This file is standalone and has no toolchain dependencies.
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal, Mapping


@dataclass(frozen=True)
class State:
    """Model state."""
    reserve_in: int
    reserve_in_before: int
    reserve_out: int
    reserve_out_before: int


@dataclass(frozen=True)
class Command:
    """Command to execute."""
    tag: Literal('swap',)
    args: Mapping[str, Any]


@dataclass(frozen=True)
class StepResult:
    """Result of a step execution."""
    ok: bool
    state: State | None = None
    effects: Mapping[str, Any] | None = None
    error: str | None = None


def init_state() -> State:
    """Return the initial state."""
    return State(
        reserve_in=1000000,
        reserve_in_before=1000000,
        reserve_out=1000000,
        reserve_out_before=1000000,
    )


def check_invariants(s: State) -> tuple[bool, str | None]:
    """Check all invariants. Returns (ok, first_failed_id)."""
    if not (isinstance(s.reserve_in, int) and not isinstance(s.reserve_in, bool) and 1 <= s.reserve_in <= 3000000000):
        return False, 'domain_reserve_in'
    if not (isinstance(s.reserve_in_before, int) and not isinstance(s.reserve_in_before, bool) and 1 <= s.reserve_in_before <= 3000000000):
        return False, 'domain_reserve_in_before'
    if not (isinstance(s.reserve_out, int) and not isinstance(s.reserve_out, bool) and 1 <= s.reserve_out <= 3000000000):
        return False, 'domain_reserve_out'
    if not (isinstance(s.reserve_out_before, int) and not isinstance(s.reserve_out_before, bool) and 1 <= s.reserve_out_before <= 3000000000):
        return False, 'domain_reserve_out_before'
    if not (((s.reserve_in <= 3000000000) and (s.reserve_in > 0))):
        return False, 'inv_reserve_in_bounded'
    if not (((s.reserve_out <= 3000000000) and (s.reserve_out > 0))):
        return False, 'inv_reserve_out_bounded'
    return True, None


def step(s: State, cmd: Command) -> StepResult:
    """Execute a step. Returns StepResult."""
    # Check pre-state invariants
    ok, failed = check_invariants(s)
    if not ok:
        return StepResult(ok=False, error=f'pre-invariant violated: {failed}')

    if cmd.tag == 'swap':
        if 'amount_in' not in cmd.args or not (isinstance(cmd.args['amount_in'], int) and not isinstance(cmd.args['amount_in'], bool) and 1 <= cmd.args['amount_in'] <= 3000000000):
            return StepResult(ok=False, error='invalid param amount_in')
        if 'min_amount_out' not in cmd.args or not (isinstance(cmd.args['min_amount_out'], int) and not isinstance(cmd.args['min_amount_out'], bool) and 1 <= cmd.args['min_amount_out'] <= 3000000000):
            return StepResult(ok=False, error='invalid param min_amount_out')
        if not ((((0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0))) < s.reserve_out) and ((cmd.args['amount_in'] + s.reserve_in) <= 3000000000) and ((s.reserve_out - (0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0)))) <= 3000000000) and (cmd.args['amount_in'] <= s.reserve_in) and ((0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0))) > 0) and (cmd.args['amount_in'] > 0) and (s.reserve_in > 0) and (s.reserve_out > 0) and (((cmd.args['amount_in'] + s.reserve_in) * (s.reserve_out - (0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0))))) >= (s.reserve_in * s.reserve_out)) and ((s.reserve_out - (0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0)))) >= 1) and ((0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0))) >= cmd.args['min_amount_out']))):
            return StepResult(ok=False, error='guard failed for swap')
        # Compute updates (simultaneous)
        new_state = State(
            reserve_in=(cmd.args['amount_in'] + s.reserve_in),
            reserve_in_before=s.reserve_in,
            reserve_out=(s.reserve_out - (0 if (cmd.args['amount_in'] + s.reserve_in) == 0 else (((cmd.args['amount_in'] * s.reserve_out) // (cmd.args['amount_in'] + s.reserve_in)) + (1 if ((cmd.args['amount_in'] * s.reserve_out) % (cmd.args['amount_in'] + s.reserve_in)) < 0 else 0)))),
            reserve_out_before=s.reserve_out,
        )
        effects = {
            'amount_out': (new_state.reserve_out_before - new_state.reserve_out),
            'k_after': (new_state.reserve_in * new_state.reserve_out),
            'k_before': (new_state.reserve_in_before * new_state.reserve_out_before),
            'new_reserve_in': new_state.reserve_in,
            'new_reserve_out': new_state.reserve_out,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    else:
        return StepResult(ok=False, error=f'unknown action: {cmd.tag}')


# === Hypothesis test scaffolding ===
# Uncomment and install hypothesis to use property-based testing

# from hypothesis import given, strategies as st
# from hypothesis.stateful import RuleBasedStateMachine, rule, initialize
#
# class ModelStateMachine(RuleBasedStateMachine):
#     def __init__(self):
#         super().__init__()
#         self.state = init_state()
#
#     @initialize()
#     def init(self):
#         self.state = init_state()
#         ok, _ = check_invariants(self.state)
#         assert ok, 'init violates invariants'
#
#     # @rule(amount_in=st.integers(), min_amount_out=st.integers())
#     # def swap(self, amount_in, min_amount_out):
#     #     cmd = Command(tag='swap', args={'amount_in': amount_in, 'min_amount_out': min_amount_out})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
# TestModel = ModelStateMachine.TestCase


def replay_trace(commands: list[tuple[str, dict[str, Any]]]) -> list[StepResult]:
    """Replay a trace of commands and return results."""
    results: list[StepResult] = []
    s = init_state()
    for tag, args in commands:
        cmd = Command(tag=tag, args=args)
        result = step(s, cmd)
        results.append(result)
        if result.ok and result.state is not None:
            s = result.state
        else:
            break
    return results


if __name__ == '__main__':
    # Quick sanity check
    s0 = init_state()
    ok, failed = check_invariants(s0)
    print(f'Initial state: {s0}')
    print(f'Invariants OK: {ok}')
    if not ok:
        print(f'  Failed: {failed}')
