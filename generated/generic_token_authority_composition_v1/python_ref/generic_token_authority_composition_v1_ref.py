"""
Auto-generated Python reference model for: generic_token_authority_composition_v1
IR hash: sha256:93724fb62b0c15b77e0cec37d21e7f0a76a1dacb0bb26e0bd22527169b4947a8

Generated by ESSO (Evolutionary Spec Search Optimizer)

This file is standalone and has no ESSO dependencies.
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal, Mapping


@dataclass(frozen=True)
class State:
    """Model state."""
    active_stake_units: int
    mint_authority_code: int
    pending_stake_units: int
    perps_units: int
    pool_units: int
    registered: int
    supply_units: int
    token_nonce: int
    wallet_units: int


@dataclass(frozen=True)
class Command:
    """Command to execute."""
    tag: Literal('activate_stake', 'burn', 'faucet_mint', 'mint', 'perps_to_wallet', 'pool_to_wallet', 'reject_invalid_projection', 'reject_unauthorized_mint', 'transfer', 'unstake', 'wallet_to_pending_stake', 'wallet_to_perps', 'wallet_to_pool')
    args: Mapping[str, Any]


@dataclass(frozen=True)
class StepResult:
    """Result of a step execution."""
    ok: bool
    state: State | None = None
    effects: Mapping[str, Any] | None = None
    error: str | None = None


def init_state() -> State:
    """Return the initial state."""
    return State(
        active_stake_units=0,
        mint_authority_code=0,
        pending_stake_units=0,
        perps_units=0,
        pool_units=0,
        registered=1,
        supply_units=0,
        token_nonce=0,
        wallet_units=0,
    )


def check_invariants(s: State) -> tuple[bool, str | None]:
    """Check all invariants. Returns (ok, first_failed_id)."""
    if not (isinstance(s.active_stake_units, int) and not isinstance(s.active_stake_units, bool) and 0 <= s.active_stake_units <= 2):
        return False, 'domain_active_stake_units'
    if not (isinstance(s.mint_authority_code, int) and not isinstance(s.mint_authority_code, bool) and 0 <= s.mint_authority_code <= 1):
        return False, 'domain_mint_authority_code'
    if not (isinstance(s.pending_stake_units, int) and not isinstance(s.pending_stake_units, bool) and 0 <= s.pending_stake_units <= 2):
        return False, 'domain_pending_stake_units'
    if not (isinstance(s.perps_units, int) and not isinstance(s.perps_units, bool) and 0 <= s.perps_units <= 2):
        return False, 'domain_perps_units'
    if not (isinstance(s.pool_units, int) and not isinstance(s.pool_units, bool) and 0 <= s.pool_units <= 2):
        return False, 'domain_pool_units'
    if not (isinstance(s.registered, int) and not isinstance(s.registered, bool) and 0 <= s.registered <= 1):
        return False, 'domain_registered'
    if not (isinstance(s.supply_units, int) and not isinstance(s.supply_units, bool) and 0 <= s.supply_units <= 2):
        return False, 'domain_supply_units'
    if not (isinstance(s.token_nonce, int) and not isinstance(s.token_nonce, bool) and 0 <= s.token_nonce <= 2):
        return False, 'domain_token_nonce'
    if not (isinstance(s.wallet_units, int) and not isinstance(s.wallet_units, bool) and 0 <= s.wallet_units <= 2):
        return False, 'domain_wallet_units'
    if not ((0 == s.mint_authority_code)):
        return False, 'inv_mint_authority_stable'
    if not ((1 == s.registered)):
        return False, 'inv_registration_stable'
    if not ((((((s.active_stake_units + s.pending_stake_units) + s.perps_units) + s.pool_units) + s.wallet_units) == s.supply_units)):
        return False, 'inv_supply_equals_accounted_units'
    return True, None


def step(s: State, cmd: Command) -> StepResult:
    """Execute a step. Returns StepResult."""
    # Check pre-state invariants
    ok, failed = check_invariants(s)
    if not ok:
        return StepResult(ok=False, error=f'pre-invariant violated: {failed}')

    if cmd.tag == 'activate_stake':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.active_stake_units) <= 2) and (s.pending_stake_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for activate_stake')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=(cmd.args['amount_units'] + s.active_stake_units),
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=(s.pending_stake_units - cmd.args['amount_units']),
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=s.wallet_units,
        )
        effects = {
            'accepted': True,
            'action_code': 10,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'burn':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not (((s.token_nonce < 2) and (1 == s.registered) and (s.supply_units >= cmd.args['amount_units']) and (s.wallet_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for burn')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=(s.supply_units - cmd.args['amount_units']),
            token_nonce=(1 + s.token_nonce),
            wallet_units=(s.wallet_units - cmd.args['amount_units']),
        )
        effects = {
            'accepted': True,
            'action_code': 3,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 1,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': -1,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'faucet_mint':
        if 'actor_code' not in cmd.args or not (isinstance(cmd.args['actor_code'], int) and not isinstance(cmd.args['actor_code'], bool) and 0 <= cmd.args['actor_code'] <= 1):
            return StepResult(ok=False, error='invalid param actor_code')
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.supply_units) <= 2) and ((cmd.args['amount_units'] + s.wallet_units) <= 2) and (1 == s.registered) and (cmd.args['actor_code'] == s.mint_authority_code))):
            return StepResult(ok=False, error='guard failed for faucet_mint')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=(cmd.args['amount_units'] + s.supply_units),
            token_nonce=s.token_nonce,
            wallet_units=(cmd.args['amount_units'] + s.wallet_units),
        )
        effects = {
            'accepted': True,
            'action_code': 2,
            'actor_code': cmd.args['actor_code'],
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 1,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'mint':
        if 'actor_code' not in cmd.args or not (isinstance(cmd.args['actor_code'], int) and not isinstance(cmd.args['actor_code'], bool) and 0 <= cmd.args['actor_code'] <= 1):
            return StepResult(ok=False, error='invalid param actor_code')
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not (((s.token_nonce < 2) and ((cmd.args['amount_units'] + s.supply_units) <= 2) and ((cmd.args['amount_units'] + s.wallet_units) <= 2) and (1 == s.registered) and (cmd.args['actor_code'] == s.mint_authority_code))):
            return StepResult(ok=False, error='guard failed for mint')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=(cmd.args['amount_units'] + s.supply_units),
            token_nonce=(1 + s.token_nonce),
            wallet_units=(cmd.args['amount_units'] + s.wallet_units),
        )
        effects = {
            'accepted': True,
            'action_code': 1,
            'actor_code': cmd.args['actor_code'],
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 1,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 1,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'perps_to_wallet':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.wallet_units) <= 2) and (s.perps_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for perps_to_wallet')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=(s.perps_units - cmd.args['amount_units']),
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=(cmd.args['amount_units'] + s.wallet_units),
        )
        effects = {
            'accepted': True,
            'action_code': 8,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'pool_to_wallet':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.wallet_units) <= 2) and (s.pool_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for pool_to_wallet')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=(s.pool_units - cmd.args['amount_units']),
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=(cmd.args['amount_units'] + s.wallet_units),
        )
        effects = {
            'accepted': True,
            'action_code': 6,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'reject_invalid_projection':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not (True):
            return StepResult(ok=False, error='guard failed for reject_invalid_projection')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=s.wallet_units,
        )
        effects = {
            'accepted': False,
            'action_code': 13,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 2,
            'rejection_noop': True,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'reject_unauthorized_mint':
        if 'actor_code' not in cmd.args or not (isinstance(cmd.args['actor_code'], int) and not isinstance(cmd.args['actor_code'], bool) and 0 <= cmd.args['actor_code'] <= 1):
            return StepResult(ok=False, error='invalid param actor_code')
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((not (cmd.args['actor_code'] == s.mint_authority_code))):
            return StepResult(ok=False, error='guard failed for reject_unauthorized_mint')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=s.wallet_units,
        )
        effects = {
            'accepted': False,
            'action_code': 12,
            'actor_code': cmd.args['actor_code'],
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 1,
            'rejection_noop': True,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'transfer':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not (((s.token_nonce < 2) and (1 == s.registered) and (s.wallet_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for transfer')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=(1 + s.token_nonce),
            wallet_units=s.wallet_units,
        )
        effects = {
            'accepted': True,
            'action_code': 4,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 1,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'unstake':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.wallet_units) <= 2) and (s.active_stake_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for unstake')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=(s.active_stake_units - cmd.args['amount_units']),
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=(cmd.args['amount_units'] + s.wallet_units),
        )
        effects = {
            'accepted': True,
            'action_code': 11,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'wallet_to_pending_stake':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.pending_stake_units) <= 2) and (s.wallet_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for wallet_to_pending_stake')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=(cmd.args['amount_units'] + s.pending_stake_units),
            perps_units=s.perps_units,
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=(s.wallet_units - cmd.args['amount_units']),
        )
        effects = {
            'accepted': True,
            'action_code': 9,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'wallet_to_perps':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.perps_units) <= 2) and (s.wallet_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for wallet_to_perps')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=(cmd.args['amount_units'] + s.perps_units),
            pool_units=s.pool_units,
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=(s.wallet_units - cmd.args['amount_units']),
        )
        effects = {
            'accepted': True,
            'action_code': 7,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    elif cmd.tag == 'wallet_to_pool':
        if 'amount_units' not in cmd.args or not (isinstance(cmd.args['amount_units'], int) and not isinstance(cmd.args['amount_units'], bool) and 1 <= cmd.args['amount_units'] <= 2):
            return StepResult(ok=False, error='invalid param amount_units')
        if not ((((cmd.args['amount_units'] + s.pool_units) <= 2) and (s.wallet_units >= cmd.args['amount_units']))):
            return StepResult(ok=False, error='guard failed for wallet_to_pool')
        # Compute updates (simultaneous)
        new_state = State(
            active_stake_units=s.active_stake_units,
            mint_authority_code=s.mint_authority_code,
            pending_stake_units=s.pending_stake_units,
            perps_units=s.perps_units,
            pool_units=(cmd.args['amount_units'] + s.pool_units),
            registered=s.registered,
            supply_units=s.supply_units,
            token_nonce=s.token_nonce,
            wallet_units=(s.wallet_units - cmd.args['amount_units']),
        )
        effects = {
            'accepted': True,
            'action_code': 5,
            'actor_code': 0,
            'amount_units': cmd.args['amount_units'],
            'nonce_delta': 0,
            'reject_code': 0,
            'rejection_noop': False,
            'supply_delta_code': 0,
        }
        ok, failed = check_invariants(new_state)
        if not ok:
            return StepResult(ok=False, error=f'post-invariant violated: {failed}')
        return StepResult(ok=True, state=new_state, effects=effects)
    else:
        return StepResult(ok=False, error=f'unknown action: {cmd.tag}')


# === Hypothesis test scaffolding ===
# Uncomment and install hypothesis to use property-based testing

# from hypothesis import given, strategies as st
# from hypothesis.stateful import RuleBasedStateMachine, rule, initialize
#
# class ModelStateMachine(RuleBasedStateMachine):
#     def __init__(self):
#         super().__init__()
#         self.state = init_state()
#
#     @initialize()
#     def init(self):
#         self.state = init_state()
#         ok, _ = check_invariants(self.state)
#         assert ok, 'init violates invariants'
#
#     # @rule(amount_units=st.integers())
#     # def activate_stake(self, amount_units):
#     #     cmd = Command(tag='activate_stake', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def burn(self, amount_units):
#     #     cmd = Command(tag='burn', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(actor_code=st.integers(), amount_units=st.integers())
#     # def faucet_mint(self, actor_code, amount_units):
#     #     cmd = Command(tag='faucet_mint', args={'actor_code': actor_code, 'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(actor_code=st.integers(), amount_units=st.integers())
#     # def mint(self, actor_code, amount_units):
#     #     cmd = Command(tag='mint', args={'actor_code': actor_code, 'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def perps_to_wallet(self, amount_units):
#     #     cmd = Command(tag='perps_to_wallet', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def pool_to_wallet(self, amount_units):
#     #     cmd = Command(tag='pool_to_wallet', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def reject_invalid_projection(self, amount_units):
#     #     cmd = Command(tag='reject_invalid_projection', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(actor_code=st.integers(), amount_units=st.integers())
#     # def reject_unauthorized_mint(self, actor_code, amount_units):
#     #     cmd = Command(tag='reject_unauthorized_mint', args={'actor_code': actor_code, 'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def transfer(self, amount_units):
#     #     cmd = Command(tag='transfer', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def unstake(self, amount_units):
#     #     cmd = Command(tag='unstake', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def wallet_to_pending_stake(self, amount_units):
#     #     cmd = Command(tag='wallet_to_pending_stake', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def wallet_to_perps(self, amount_units):
#     #     cmd = Command(tag='wallet_to_perps', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
#     # @rule(amount_units=st.integers())
#     # def wallet_to_pool(self, amount_units):
#     #     cmd = Command(tag='wallet_to_pool', args={'amount_units': amount_units})
#     #     result = step(self.state, cmd)
#     #     if result.ok:
#     #         self.state = result.state
#
# TestModel = ModelStateMachine.TestCase


def replay_trace(commands: list[tuple[str, dict[str, Any]]]) -> list[StepResult]:
    """Replay a trace of commands and return results."""
    results: list[StepResult] = []
    s = init_state()
    for tag, args in commands:
        cmd = Command(tag=tag, args=args)
        result = step(s, cmd)
        results.append(result)
        if result.ok and result.state is not None:
            s = result.state
        else:
            break
    return results


if __name__ == '__main__':
    # Quick sanity check
    s0 = init_state()
    ok, failed = check_invariants(s0)
    print(f'Initial state: {s0}')
    print(f'Invariants OK: {ok}')
    if not ok:
        print(f'  Failed: {failed}')
