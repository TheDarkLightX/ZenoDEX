[package]
name = "zenodex-zdex-tokenomics-lane-coordinator-risc0-methods"
version = "0.1.0"
edition = "2021"
rust-version = "1.90"
publish = false
description = "Pinned ZDEX tokenomics lane coordinator guest ELF and image ID"

[build-dependencies]
risc0-build = "=3.0.6"

[lib]
doctest = false

[package.metadata.risc0]
methods = ["guest"]
