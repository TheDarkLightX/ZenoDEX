# Full local/dev install convenience manifest.
# Production/container runtime should use `requirements-core.txt`.
-r requirements-core.txt
-r requirements-agents.txt

# Test tooling commonly needed in local checkouts.
pytest>=7.4.0
pytest-cov>=4.1.0
