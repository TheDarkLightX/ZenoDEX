services:
  zenodex-local:
    build:
      context: .
      dockerfile: Dockerfile.operator-tools
    image: zenodex/operator-tools:local
    command: ["tools/zeno_ledger_public_network_smoke.py", "--help"]
