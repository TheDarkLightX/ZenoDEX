# Retired local Tau operator overlay tombstone.
#
# Current Tau is incompatible with the historical application bridge. The
# former local-node service is deliberately absent. Historical replay tooling
# remains research-only and cannot be selected through this Compose file.

services: {}
