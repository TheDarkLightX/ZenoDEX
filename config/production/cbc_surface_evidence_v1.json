{
  "schema": "zenodex/cbc-surface-evidence/v1",
  "scope_id": "spot_dex",
  "claim_scope": ["cpmm_swap", "balances", "state_root", "nonces"],
  "claim_scope_note": "The authority surfaces ANDed into production_security_claim. The gate fails closed if this does not match the registry's non-evidence_only rows (so a real surface cannot be silently dropped by mismarking it evidence_only). Rows with claim_role=evidence_only (e.g. replay_guard) are retained as proof-carriers attached to an authority surface but excluded from the claim.",
  "note": "HONEST work-tracker for the spot-DEX production-claim scope (Phase 0 of docs/PRODUCTION_PROMOTION_PLAN.md). Every column is verified:false until its artifact is GENUINELY checked and bound to the running implementation — refs below are CANDIDATE artifacts, not cleared evidence. The gate (tools/gate_cbc_matrix_closure.py) computes the per-surface and scope claims from this file; it fails closed while any column is unverified. Flip a column to verified:true ONLY when a replayable check proves it clears, per the per-surface promotion checklist. Do not hand-set verified:true.",
  "surfaces": {
    "cpmm_swap": {
      "running_impl": {"ref": "src/core/cpmm.py (LIVE swap math; called on the authority path by src/core/settlement_strong_validator.py + src/core/amm_dispatch.py; delegates to src/kernels/python/cpmm_swap_v8.py)", "verified": true, "note": "CLEARED (Phase 4, dual review BOTH endorsed): src/core/cpmm.py is the LIVE swap computation (not a store or off-path reference) — amm_dispatch.swap_exact_in/out_for_pool routes CPMM swaps through it and settlement_strong_validator.validate_settlement_strong replays value-moving swaps through that path. It delegates to the shared cpmm_swap_v8 kernel that the PR-gated differential proves vs the Rust cpmm_swap, and Phase 4 added cpmm.py + cpmm_swap_v8 to the runtime-shadow CI filter — so the running impl is identified, bound to the proven kernel via the shared import, and CI-gated."},
      "formal_spec": {"ref": "docs/assurance/cpmm_swap_formal_spec_contract.json; tools/check_cpmm_swap_formal_spec_contract.py; lean-mathlib/Proofs/CPMMInvariants.lean; lean-mathlib/Proofs/CpmmSwapV8ExactInAdmissibility.lean; lean-mathlib/Proofs/CpmmSwapV8ExactOutMinimality.lean", "verified": true, "note": "[OWNER-AUTHORIZED 2026-06-05 — Option A elected. The repo owner authorized the Codex evidence increment AND this matrix-semantics decision: for the nonlinear-u256 CPMM swap surface the formal spec IS the Lean 4 theorem-statement contract (Option A from internal/CPMM_FORMALSPEC_MATRIX_FINDING.md).] Checker TEETH-VERIFIED by adversarial audit wo9s2w6uh: a mutated or renamed Lean theorem is caught by the contract hash-pin + the spot-proof receipt + the declaration-token check — NOT a config-mirror (materially unlike the reverted state_root formal_spec). This INTENTIONALLY shares the Lean evidence with proof_artifact (statements-as-spec vs proof+binding); the owner accepts that column collapse for nonlinear surfaces where a separate spec-language statement is infeasible (Tau bv32 « u256, ESSO NIA-timeout). ORIGINAL: CLEARED (Codex evidence increment): the matrix decision is now explicit and source-pinned — for the nonlinear u256 CPMM swap surface, the formal spec is the Lean 4 contract made of theorem statements and definitions, not the withdrawn Tau/ESSO placeholders. docs/assurance/cpmm_swap_formal_spec_contract.json pins the normative claims: exact-in output formula, exact-out gross-input sufficiency/minimality, fee rounding, and k monotonicity. tools/check_cpmm_swap_formal_spec_contract.py verifies the contract, rejects claim/source-hash drift, rejects missing Lean declaration tokens, requires the public spot-proof receipt for the CPMM proof ids, and rejects the withdrawn cpmm_output_amount_v2/ref YAMLs as spec evidence. runtime-shadow.yml and release-integrity.yml both run the checker and its negative tests. The proof/build and live-kernel binding remain separately covered by proof_artifact, differential_tests, runtime_invariants, and authority_mode."},
      "proof_artifact": {"ref": "lean-mathlib/Proofs/CPMMInvariants.lean; lean-mathlib/Proofs/CpmmSwapV8ExactInAdmissibility.lean; lean-mathlib/Proofs/CpmmSwapV8ExactOutMinimality.lean (committed-receipt pinned, BUILT_NO_SORRY); tests/runtime/test_cpmm_v8_exact_out_lean_property_binding.py + test_cpmm_v8_exact_in_lean_property_binding.py + test_cpmm_v8_exact_out_k_conservation_binding.py (PR-gated live property bindings)", "verified": true, "note": "CLEARED (autonomous run 2026-06-04; TRIPLE-REVIEWED + CONFIRMED: adversarial re-rule = FLIP high-conf + Gemini = FLIP high-conf + Codex final-confirm = A- (accepts the scoped swap-math claim, would NOT revert), all independently teeth-verified; Codex additionally hardened the receipt source-binding envelope in be2e5d98). The cpmm swap MATH is now PROOF-BOUND to the running cpmm_swap_v8 kernel: (1) the Lean proofs — CPMMInvariants.k_monotone_{zero_fee,with_fee} (kValue_new>=kValue_old DERIVED from Nat.div_mul_le_self, NOT a ValidSwap-assumes-K tautology), CpmmSwapV8ExactOutMinimality.swap_exact_out_sufficient_and_minimal, CpmmSwapV8ExactInAdmissibility — are BUILT_NO_SORRY (axiom-audited: zero sorryAx) and hash-pinned in the committed spot-proof receipt, CI-checked per-PR by runtime-shadow.yml + test_spot_proof_public_receipt.py WITHOUT the prover toolchain; (2) three PR-gated, teeth-verified binding tests (tests/runtime/**, run by runtime-shadow's `pytest tests/runtime`) pin the LIVE kernel to INDEPENDENT Python transcriptions of the Lean formula defs (non-circular): exact-out output sufficiency+minimality (g=0..1e26 incl the 2**53 float-collapse zone), exact-out k-conservation (independent reserve recompute — catches a drained new_reserve_in even with a self-consistent k_after field), and exact-in output + k-nondecrease across protocol-fee-share 0..100%. k-nondecrease is bound BOTH directions on the PROVEN net-retained k_monotone core; the pshare>0 case is a sweep-confirmed COROLLARY (new_rin=rin+net_in+lp_fee >= rin+net_in since lp_fee>=0), NOT separately mechanized for all pshare. The protocol-fee SPLIT value is out of THIS column's scope (k-safety only needs protocol_fee<=fee_total, kernel-guarded) — split correctness belongs to differential_tests/formal_spec. REVIEW [B -> A-]: the proof note previously carried stale text from before the owner-authorized formal_spec flip. SCOPE (this column = swap-math proof binding): reject-precedence is covered by differential_tests (cleared), state-root binding is the separate state_root surface, and the formal-spec cross-check is covered by the separate owner-authorized formal_spec column. cpmm_output_amount_v2.yaml remains a withdrawn placeholder (not evidence)."},
      "differential_tests": {"ref": "tests/runtime/test_cpmm_settlement_conformance.py (Python<->Rust cpmm_swap.rs, CI runtime-shadow.yml)", "verified": true, "note": "CLEARED (Phase 4): genuine cross-implementation differential — the conformance test drives the runtime swap (settlement_swap_runtime_v1 -> cpmm_swap_v8) vs the compiled Rust cpmm_swap, reject-domain + reserve-domain parity. PR-gated by runtime-shadow.yml: Phase 4 ADDED src/core/cpmm.py + src/kernels/python/cpmm_swap_v8.py to its trigger paths (Codex review found them missing — the filter previously only had settlement_swap_runtime_v1.py, so a live-math regression would not have fired the differential). Now a change to the live swap math triggers it. The LIVE cpmm.py + the conformance runtime call the SAME cpmm_swap_v8 kernel proven vs the Rust cpmm_swap, so it binds the running code. 3 passed. Caveat (Codex): the differential exercises settlement_swap_runtime_v1 -> cpmm_swap_v8 (the SHARED KERNEL / live swap math), not every cpmm.py wrapper behavior (e.g. protocol-fee wrapping) — those wrapper paths are not differentially tested here."},
      "runtime_invariants": {"ref": "in-path k_after >= k_before guards in src/core/cpmm.py + src/kernels/python/settlement_swap_runtime_v1.py; tests/runtime/test_cpmm_settlement_semantic_invariants.py", "verified": true, "note": "CLEARED (Phase 4, dual review BOTH endorsed): the k-invariant (k_after >= k_before) is ENFORCED IN-PATH in src/core/cpmm.py + src/kernels/python/settlement_swap_runtime_v1.py before state commit (not just property tests); the uniform-batch lane carries its own aggregate CPMM invariant; production boundary checks reject legacy/direct-apply exposure. Now CI-gated (runtime-shadow covers cpmm.py + cpmm_swap_v8 + the semantic-invariant tests), so the guards cannot be silently removed without breaking the build."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py; tests/runtime/test_cpmm_settlement_live_path.py", "verified": true, "note": "CLEARED (Phase 4): the cpmm settlement swap routes through active_mode(CPMM_SETTLEMENT_SURFACE) + decide() (settlement_swap_runtime_v1.py:351,377), defaulting to pure Python under python_authority and failing closed on disagreement/Rust-error/timeout; production-strict.yaml per_surface {} / promoted_surfaces []. test_cpmm_settlement_live_path.py exercises the routing + fail-closed. CI-gated by runtime-shadow.yml. Declared mode honestly equals what runs."},
      "open_gaps_closed": true,
      "resolved_by_review": "Phase 4 RE-REVIEW cleared running_impl + runtime_invariants after the CI fix. Phase 5 proof_artifact was later cleared by triple-reviewed Lean proof and runtime binding evidence. Codex formal-spec increment closes the last cpmm row gap by making the nonlinear-u256 formal spec role explicit: Lean theorem statements and definitions are the spec contract, while the withdrawn Tau/ESSO placeholders remain excluded. check_cpmm_swap_formal_spec_contract.py is PR/release-gated and fails closed on claim drift, source drift, missing Lean declarations, missing spot proof ids, workflow drift, or placeholder-spec reuse. cpmm_swap is now 7/7; this does not clear balances or nonces.",
      "_round1_review_question": "Phase 4 (branch claude/prod-promotion-phase4-cpmm). ROUND-1 dual review: Codex C / Gemini A. Codex was factually right on two points, both addressed: (a) the runtime-shadow CI filter did NOT include src/core/cpmm.py or src/kernels/python/cpmm_swap_v8.py, so the differential would not have fired on a live-swap-math regression — FIXED (both paths added to the pull_request + push filters), making differential_tests genuinely PR-gated for the live math; (b) runtime_invariants 'not enforced' was wrong — in-path k_after>=k_before guards DO exist in cpmm.py + settlement_swap_runtime_v1.py — note corrected. authority_mode confirmed. RE-REVIEW QUESTION: with CI now covering cpmm.py + cpmm_swap_v8, does running_impl flip (cpmm.py is the live swap computation settlement calls; CI now gates it; the differential binds the shared kernel) and does runtime_invariants flip (in-path k-guards, now CI-gated, covering all settlement paths)? proof_artifact stays false (Lean/ESSO not PR-gated)."
    },
    "balances": {
      "running_impl": {"ref": "src/state/balances.py (BalanceTable; LIVE store via state.balances in dex_engine.py + state_root.py); src/core/batch_clearing.py::apply_settlement (LIVE settlement transition); src/core/balance_kernel.py (CBC-core credit/transfer reference)", "verified": true, "note": "[FLIPPED 2026-06-05 — DUAL-CONFIRMED: adversarial flip-ruling wyi8a1759 + independent Codex (both FLIP; isolation teeth: a +1 leak with the in-path guard NEUTERED is still caught by the binding's own re-summed oracle); owner-authorized] balances.py BalanceTable is the live balance store and batch_clearing.apply_settlement is the live settlement transition boundary. Codex follow-up added a transactional apply path: declared deltas must conserve, the mutation runs on scratch copies, actual user+pool per-asset totals are recomputed from post-state, and the original tables commit only after the postcondition holds. REVIEW [B -> A-]: this closes the prior live-path leak class where declared deltas conserved but an implementation bug could create value. Bound to the live transition by tests/runtime/test_settlement_supply_conservation_lean_binding.py (0 balance_kernel refs — clears the prior off-path KEEP_FALSE w3x2g2pss). Non-blocking coverage caveat (same bar runtime_invariants cleared at): single-intent corpus; multi-intent/COW covered by the arbitrary-length theorem, not yet exercised by the binding."},
      "formal_spec": {"ref": "docs/assurance/settlement_supply_formal_spec_contract.json; tools/check_settlement_supply_formal_spec_contract.py; lean-mathlib/Proofs/SettlementSupplyConservation.lean; tests/runtime/test_settlement_supply_conservation_lean_binding.py", "verified": true, "note": "[FLIPPED 2026-06-05 — OWNER-AUTHORIZED Option A + teeth-verified contract; flip-ruling wyi8a1759 NEEDS_BUILD satisfied] The balances formal spec follows the owner-authorized Option-A pattern used for CPMM. The settlement_supply_formal_spec_contract pins Lean 4 definitions/theorem statements as the formal spec for unbounded per-cell settlement supply conservation; its checker rejects source drift, missing Lean declaration tokens, missing kernel-assurance proof ids, forbidden bounded Tau refs, workflow-gate drift, and a comment-only sigma-delta nontautology bypass. Unbounded multi-owner batch arity (variable-length deltas; supply_applyDeltas is induction over a List Int of arbitrary length) is inexpressible in bounded Tau bv32/ESSO, so the Lean theorem-statement contract IS the formal spec; bounded balance_transition_v1.tau/balance_safety_v1.tau are forbidden_spec_refs (width-cast fake-green). Teeth-verified (10 negative tests; a LIVE theorem rename is double-caught by the contract pin + the kernel-assurance receipt). Shares the Lean file with proof_artifact (column collapse, as owner-authorized for cpmm)."},
      "proof_artifact": {"ref": "rust-runtime/crates/zenodex-runtime-core/src/balance_kernel.rs (7 Kani harnesses, committed-receipt pinned by docs/assurance/kernel_assurance_public_receipt.json + tools/kernel_assurance_manifest.json); lean-mathlib/Proofs/SettlementSupplyConservation.lean (committed-receipt pinned); tests/runtime/test_settlement_supply_conservation_lean_binding.py; tests/runtime/test_balance_kernel_conformance.py; tests/runtime/test_balance_kernel_live_binding.py", "verified": true, "note": "[FLIPPED 2026-06-05 — DUAL-CONFIRMED: flip-ruling wyi8a1759 (FLIP high) + independent Codex (FLIP A-), both teeth-verified incl the ISOLATION test (the binding catches a +1 supply leak even with the in-path conservation guard NEUTERED — a proof->live refinement, not a guard-mirror); owner-authorized] balance_kernel.rs's 7 Kani harnesses remain receipt-pinned, and SettlementSupplyConservation.lean is in the kernel-assurance receipt. That Lean proof derives supply(applyDeltas L D)=supply L+supply D by induction over arbitrary per-cell deltas, then proves accepted(Σbalance_deltas+Σreserve_deltas=0) implies combined user+pool supply preservation. REVIEW [A- -> A]: the witness lemmas were changed from native_decide to kernel-checked arithmetic so Lean.trustCompiler is not in the dependency surface. tests/runtime/test_settlement_supply_conservation_lean_binding.py binds the theorem model to live validate+apply with independent pre/post cell deltas and a leak mutation test. Axiom profile [propext, Classical.choice, Quot.sound] only (no native_decide/trustCompiler). NON-TAUTOLOGICAL (both reviewers executed it): removing the accepted(Σdelta=0) hypothesis makes omega FAIL with a counterexample, so the gate is load-bearing and supply-preservation is DERIVED. DISTINCT from runtime_invariants (the in-path guard): the binding's independent oracle catches a leak with that guard disabled. CI-gated: kernel-assurance receipt source-pin (hash matches) + BUILT_NO_SORRY + manifest-pinned theorem list; binding PR-gated via tests/runtime/**."},
      "differential_tests": {"ref": "tests/runtime/test_balance_kernel_conformance.py (Python<->Rust); tests/runtime/test_balance_kernel_live_binding.py (Python-core<->live BalanceTable); CI runtime-shadow.yml", "verified": true, "note": "CLEARED (Phase 3, dual-review endorsed by both Codex + Gemini): two PR-gated (runtime-shadow.yml, triggers on balance_kernel.py / balances.py / rust-runtime / tests/runtime) differentials span both axes — conformance binds balance_kernel.py to the compiled Rust balance_kernel (reject-code parity + 400-tx fuzz), and live_binding binds balance_kernel.py to the LIVE BalanceTable store (identical (pubkey,asset)->amount maps + supply conservation; shared domain explicit). Conformance alone does not reach the live store — the pair does (Codex condition)."},
      "runtime_invariants": {"ref": "src/core/batch_clearing.py::_check_settlement_asset_delta_conservation + transactional apply_settlement postcondition; docs/assurance/settlement_conservation_live_refinement_receipt.json; tests/runtime/test_settlement_conservation_live_binding.py; tools/check_settlement_conservation_live_refinement.py", "verified": true, "note": "CLEARED and strengthened by Codex: asset conservation is enforced before mutation from settlement deltas and after mutation from actual state. apply_settlement now applies on scratch copies, recomputes per-asset totals across user balances plus pool reserves, rejects any post-state mismatch, and commits only after the postcondition holds. test_apply_settlement_postcondition_rejects_internal_balance_leak mutates the private scratch applier after a conservative settlement and proves the public entrypoint rejects without committing caller state. The committed settlement_conservation_live_refinement_receipt was regenerated after the source change and still checks cleanly. Remaining balances gaps stay false pending review of running_impl/formal_spec/proof_artifact."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py; tests/runtime/test_balance_kernel_live_path.py", "verified": true, "note": "CLEARED (Phase 3): python_authority is the source default (authority.py DEFAULT_MODE), fail-closed on disagreement/Rust-error/timeout/malformed (no silent fallback); production-strict.yaml per_surface {} / promoted_surfaces []. tests/runtime/test_balance_kernel_live_path.py proves credit/transfer route to pure-Python under PYTHON_AUTHORITY and FAIL CLOSED (AuthorityError) on injected disagreement or missing Rust binary; BalanceTable itself has zero Rust coupling. CI-gated by runtime-shadow.yml. The declared mode honestly equals what runs."},
      "open_gaps_closed": true,
      "resolved_by_review": "FLIPPED 2026-06-05 to 7/7. proof_artifact + running_impl DUAL-CONFIRMED by adversarial flip-ruling wyi8a1759 (FLIP) + independent Codex (FLIP A-), both EXECUTING the decisive isolation teeth (a +1 supply leak with the in-path conservation guard NEUTERED is still caught by the SettlementSupplyConservation binding's independently-re-summed oracle), clearing the prior w3x2g2pss off-path KEEP_FALSE. formal_spec OWNER-AUTHORIZED Option A (Lean theorem-statement contract for the unbounded multi-owner batch-arity surface, Tau/ESSO inexpressible) + teeth-verified checker. Gate stays production_security_claim=False: nonces still blocks. PRIOR: Phase 3 dual review (Codex B / Gemini A) reconciled to the conservative ruling. Later Codex follow-ups strengthened the live settlement boundary with a transactional postcondition and added a non-tautological SettlementSupplyConservation Lean proof to the kernel-assurance receipt. Claude/Codex added a source-pinned SettlementSupplyConservation Option-A formal-spec contract and Codex hardened/wired it into PR/release gates, but formal_spec remains false pending dual review and final matrix-decision acceptance. Net remains balances 3/7 until running_impl/formal_spec/proof_artifact receive review-backed flips."
    },
    "state_root": {
      "running_impl": {"ref": "src/state/state_root.py; src/integration/zeno_ledger_v0.py::dex_state_root_v0; docs/assurance/state_root_surface_evidence_receipt.json", "verified": true, "note": "[FLIPPED 2026-06-05 — TRIPLE-CONFIRMED: 3-agent adversarial re-audit w2ewpm3je (exact-mutation replay) + direct HEAD teeth re-confirm + Gemini independent code-review, all FLIP high-conf; builder Codex left ready-for-review without self-flipping] CLEARED (Phase 4): src/state/state_root.py is the live v5 spot-root implementation; dex_state_root_v0 is the ledger adapter that rejects non-spot vault/oracle/perps lanes before committing it. The state-root surface receipt pins these source hashes and the implementation refs."},
      "formal_spec": {"ref": "src/kernels/dex/state_root_v5_scope_contract.json; tools/check_state_root_surface_evidence.py; tests/test_check_state_root_surface_evidence.py; docs/assurance/state_root_surface_evidence_receipt.json", "verified": true, "note": "[FLIPPED 2026-06-05 — TRIPLE-CONFIRMED: re-audit w2ewpm3je + HEAD teeth re-confirm (byte-contract ISOLATION: a live _encode_pools_section reserve0/reserve1 swap is caught with the source-token scan AND hash-pin disabled) + Gemini independent code-review, all FLIP high-conf] The wsxychdeg config-mirror bug is fixed in the receipt checker: the JSON section contract is now independently encoded over nontrivial BAL/POL/LPB/LPA/NNC/FEE witness states and compared byte-for-byte with src.state.state_root's live section encoders. Negative tests prove empty section bodies fail, reserve0/reserve1 spec swaps fail, and a live Python pool encoder reserve-order mutation fails. Rust encoder source-token order is also pinned for the same field order. Residual (non-blocking, documented): the 5 fixed witnesses are not exhaustive, and the Rust-side encoder is bound by source-token order only (weaker than the Python byte-contract)."},
      "proof_artifact": {"ref": "tools/runtime/state_root_injectivity.py; rust-runtime/crates/zenodex-runtime-core/src/state_root.rs::kani_contracts; docs/assurance/state_root_surface_evidence_receipt.json", "verified": true, "note": "CLEARED (Phase 4): the receipt records VERIFIED cargo-kani 0.60.0 results for 7 state_root guard harnesses (fee_bps, nonce bound, LP duration presence, pool asset order, status codes, cover reachability) and the Python preimage-injectivity proof obligations (framing decoder left inverse, uvarint injectivity, bounded no-collision including FEE). Scope: proves v5 preimage/guard obligations under SHA-256 collision resistance; it does not claim a full-app root for zUSD/perps/oracle/vault lanes."},
      "differential_tests": {"ref": "tests/runtime/test_state_root_vectors.py; tests/runtime/test_state_root_curve_config_grid.py; tests/runtime/test_state_root_live_path.py; .github/workflows/runtime-shadow.yml; docs/assurance/state_root_surface_evidence_receipt.json", "verified": true, "note": "CLEARED (Phase 4): Python/Rust v5 state-root vectors, randomized corpus, curve-config boundary grid, and live authority-path tests are PR-gated by runtime-shadow.yml. The receipt pins the test refs and workflow hash so this differential lane cannot silently drift."},
      "runtime_invariants": {"ref": "src/integration/zeno_ledger_v0.py::validate_block_state_transition_v0; tools/zeno_ledger_run_local.py::build_local_block_v0; tools/zeno_ledger_node.py::_validate_live_block_report_state_roots_v0; tests/integration/test_zeno_ledger_post_state_root_binding_v0.py; tests/integration/test_zeno_ledger_node_state_root_binding.py; docs/assurance/state_root_surface_evidence_receipt.json", "verified": true, "note": "[FLIPPED 2026-06-05 — TRIPLE-CONFIRMED: re-audit w2ewpm3je (deleting either live wire turns the binding tests red) + HEAD confirm + Gemini independent (all 4 _write_live_state sites guarded), all FLIP high-conf] The pure DEX transition checker is now on the live producer path through build_local_block_v0 before block artifacts commit. Node append/follower acceptance also validates header/body roots plus pre_state_root/post_state_root against the exact local pre/post state files before live_state.json advances; failed validation discards speculative artifacts. Tests prove the direct validator, live DEX append wiring, and a forged faucet header with self-consistent app_hash/header_hash but wrong post_state_root rejects before the live pointer moves. Scope caveat: the custom local-testnet faucet/reward/tokenomics sidecar paths are rooted by the pre/post file binding, while the full settlement validate+apply replay theorem remains the DEX transition path."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; config/deploy/public-testnet.yaml; src/runtime/authority.py; tests/runtime/test_state_root_live_path.py; .github/workflows/runtime-shadow.yml; docs/assurance/state_root_surface_evidence_receipt.json", "verified": true, "note": "[FLIPPED 2026-06-05 — TRIPLE-CONFIRMED: re-audit w2ewpm3je (binary-absent => rust profile FAILS \"differential reported skipped tests\") + HEAD teeth re-confirm + Gemini independent code-review, all FLIP high-conf] The Rust fail-closed disagreement test is moved out of the Python-only runtime job and into the python-rust-shadow job after cargo builds zenodex-runtime. The receipt checker has explicit python/rust/all required-test profiles: python-runtime runs the Python evidence profile, python-rust-shadow runs the Rust authority profile, and release-integrity runs the full profile. Workflow regression tests pin the ignore/list placement so the load-bearing Rust test cannot silently skip in PR CI. Residual (non-blocking): test_state_root_authority_mode_runs_only_in_rust_shadow_job asserts string-presence not structural placement; the checker skip-detector is the load-bearing backstop."},
      "open_gaps_closed": true,
      "blocking_gap": "",
      "resolved_by_review": "FLIPPED 2026-06-05 to 7/7 (running_impl, formal_spec, runtime_invariants, authority_mode) after TRIPLE confirmation: 3-agent adversarial re-audit w2ewpm3je replayed the exact prior corruptions (reserve0/reserve1 swap caught by the byte-contract under isolation; self-consistent forged post_state_root rejected before the live pointer moves; binary-absent rust profile fails on skipped load-bearing tests) + direct HEAD teeth re-confirm + Gemini independent code-review, ALL FLIP high-conf. Builder Codex (eef993ea) correctly left the columns ready-for-review without self-flipping. Gate stays production_security_claim=False (balances + nonces still open). Teeth tests: empty included_sections fails, reserve0/reserve1 spec swap fails, live Python pool encoder reserve-order mutation fails, Rust encoder token-order mutation fails, forged post_state_root with recomputed app_hash/header_hash fails before live_state.json advances, workflow placement tests fail if the Rust authority test moves back to Python-only CI. Live/CI binding: running_impl is pinned to src/state/state_root.py plus the ledger adapter; formal_spec binds src.state.state_root section encoders through tools/check_state_root_surface_evidence.py; runtime_invariants reaches tools/zeno_ledger_run_local.py::build_local_block_v0 and tools/zeno_ledger_node.py::_validate_live_block_report_state_roots_v0; authority_mode runs tests/runtime/test_state_root_live_path.py in python-rust-shadow after cargo build. runtime-shadow.yml gates PR drift with python/rust receipt profiles, and release-integrity.yml runs the full receipt check. Double-counting caveat: proof_artifact and differential_tests remain separate already-cleared receipt-backed evidence; the newly flipped columns rely on the checker/live-path/CI-placement teeth above. Review grade: B -> A- after stale post-flip narrative was corrected to match the computed matrix."
    },
    "nonces": {
      "running_impl": {"ref": "src/state/nonces.py (validate_and_apply_intent_nonce_batch; LIVE authority via dex_engine.py:132, require_all_nonces=True)", "verified": false, "note": "Identified as the live spot replay authority. Pending: bind the proof chain (below) to the multi-sender BATCH wrapper, not just the single-transition slice."},
      "formal_spec": {"ref": "src/kernels/dex/nonce_batch_sequencing_v1.yaml (ESSO; z3+cvc5 VERIFIED); lean-mathlib/Proofs/ZenoDEXNonceBatchWrapper.lean; src/tau_specs/recommended/nonce_replay_guard_v1.tau", "verified": false, "note": "STRONG EVIDENCE, not yet a cleared surface spec (peer review: an initial verified:true flip was an overclaim and was reverted). The tracked ESSO model nonce_batch_sequencing_v1.yaml formally specifies + machine-proves (validate + verify-multi z3+cvc5 VERIFIED, both solvers agree, deterministic) the per-sender SINGLE-TRANSITION strict-sequential policy. CODEX REVIEW [A- -> A]: Proofs.ZenoDEXNonceBatchWrapper now formalizes the multi-sender sorted-fold batch wrapper with the exact non-circular theorem shape: canonicalBatchAccepts(batch)=true -> every accepted sender group is exactly the successor range {last+1, ..., last+k} and groupFinal=last+k, plus the older safety/Nodup facts. tests/runtime/test_nonces_batch_wrapper_lean_property_binding.py binds the LIVE validate_and_apply_intent_nonce_batch accept/reject decision and final per-sender state to an independent Python transcription of that Lean sorted-fold law over a finite domain sweep, with planted broken validators for gap acceptance and partial-apply-on-reject. STILL FALSE: this is scoped wrapper-law formalization over canonical sender/positive-u32 inputs and a PR-gated finite binding, pending dual review and full surface-spec ruling."},
      "proof_artifact": {"ref": "src/kernels/dex/nonce_batch_sequencing_v1.yaml (verify-multi z3+cvc5 VERIFIED); lean-mathlib/Proofs/ZenoDEXNonceBatchWrapper.lean (kernel-assurance committed receipt); tests/runtime/test_nonce_esso_model.py; tests/runtime/test_nonces_batch_wrapper_lean_property_binding.py; rust-runtime/crates/zenodex-runtime-core/src/replay_guard.rs (4 Kani, CI runtime-kani.yml)", "verified": false, "note": "STRONG but still withheld. The single-transition ESSO proof is genuine and PHASE 5 already pinned it in the SPOT proof committed receipt. CODEX REVIEW [A- -> A]: the Lean nonce-batch-wrapper proof is now stronger than the earlier safety-only surface; the committed kernel-assurance receipt pins exact-range theorems including canonical_batch_accept_decision_implies_exact_ranges, acceptsSortedFold_eq_successorRange, and acceptsSortedFold_final_eq_start_add_length. release-integrity.yml checks the kernel assurance receipt, and runtime-shadow.yml watches lean-mathlib/Proofs/ZenoDEXNonceBatchWrapper.lean plus the receipt/checker/manifest paths for per-PR drift checks without running Lean. The live binding test is PR-gated under tests/runtime and has mutation-proven teeth. STILL FALSE pending dual review, cross-language batch differential sufficiency review, and a final ruling that the proof artifact need not cover every malformed-input/reject-reason branch."},
      "differential_tests": {"ref": "tests/runtime/test_nonce_batch_conformance.py (src.state.nonces live batch authority <-> Rust nonce-batch-op); tests/runtime/test_replay_guard_conformance.py (single-transition reference parity); runtime-shadow.yml", "verified": true, "note": "CLEARED (Codex nonce-batch differential increment): a new typed Rust batch shadow (rust-runtime/crates/zenodex-runtime-core/src/replay_guard.rs::validate_nonce_batch, exposed by zenodex-runtime nonce-batch-op) now differentially checks the LIVE Python authority src.state.nonces.validate_and_apply_intent_nonce_batch, not only the single-transition replay_guard reference. tests/runtime/test_nonce_batch_conformance.py covers accepting no-op/out-of-order/multi-sender/u32-boundary batches, every live reject class, nonce-before-sender precedence, shape-before-duplicate/range semantic precedence, reject-is-no-op state entries, invalid JSON scalar nonce values, non-string senders, and a deterministic randomized boundary corpus. The test caught and fixed two Rust parser precedence bugs before this flip: later invalid_nonce could incorrectly beat earlier invalid_sender, and later invalid_sender could incorrectly beat earlier missing_nonce under require_all_nonces=true. CODEX REVIEW [B+ -> A-]: the note previously said first-sender precedence, which was too broad; the live consensus order validates nonce/sender shape across the input batch before grouped duplicate/range checks. Remaining nonces gaps stay false: running_impl/formal_spec/proof_artifact/open_gaps_closed."},
      "runtime_invariants": {"ref": "src/state/nonces.py::_check_nonce_batch_runtime_invariants; tests/runtime/test_nonces_batch_binding.py; tests/runtime/test_nonces_batch_wrapper_lean_property_binding.py; runtime-shadow.yml", "verified": true, "note": "CLEARED (Codex nonces increment): validate_and_apply_intent_nonce_batch now runs an explicit accept-path postcondition before returning the staged NonceTable. _check_nonce_batch_runtime_invariants verifies that the staged table is exactly the canonical per-sender contiguous advance from the input table and that untouched senders remain unchanged; failures return ok=false with no staged table. tests/runtime/test_nonces_batch_binding.py covers the helper's accepting case and a partial-staged-advance rejection, plus the existing reject-is-no-op, per-sender isolation, cross-sender atomicity, strict-sequential, duplicate, precedence, and bounds tests. PR-gated by runtime-shadow.yml through src/state/nonces.py and tests/runtime/**. REVIEW [B -> A-]: the earlier note still listed differential_tests as a remaining gap after that column had cleared; keep the release-gate narrative aligned with the computed matrix. Remaining nonces gaps are running_impl/formal_spec/proof_artifact/open_gaps_closed, because this is runtime enforcement plus property coverage, not the full machine-checked batch refinement."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py", "verified": true, "note": "CLEARED (Phase 2): python_authority is the source default (authority.py DEFAULT_MODE) and is fail-closed on Python/Rust disagreement, Rust error, timeout, or malformed output ('No silent fallback. Unsupported modes raise.'). production-strict.yaml declares per_surface {} / promoted_surfaces []. nonces.py has ZERO Rust/authority coupling (grep-confirmed) so the live batch path cannot be flipped. CI-gated by runtime-shadow.yml (test_authority_selector + deploy-profile tests, 91 passing) which load the real on-disk profile. The declared mode honestly equals what runs."},
      "open_gaps_closed": false,
      "blocking_gaps": "Phase 2 added tests/runtime/test_nonces_batch_binding.py (now 41 tests, CI-gated by runtime-shadow.yml) which PINS the batch semantics of the live authority and DOWNGRADES both gaps from unproven to tested-but-not-formally-proven: (1) reject-precedence is now CANONICALIZED to the authority's order (nonce-before-sender) and tested (test_canonical_precedence_is_nonce_before_sender); the Phase-1 single-transition binding is correctly scoped to single-fault inputs because the replay_guard reference checks sender-first. (2) cross-sender ATOMICITY, ORDER-INDEPENDENCE, in-batch DEDUP, per-sender ISOLATION, and reject-is-NO-OP are pinned as deterministic property tests, and the multi-sender WRAPPER LAW binds the batch accept+final-state to a per-sender sorted fold of the proven single-transition admit core. PROGRESS: Phase 2b added the ESSO model nonce_batch_sequencing_v1.yaml (z3+cvc5 VERIFIED inductive) that machine-proves the per-sender SINGLE-TRANSITION sequencing core (gapless-prefix/anti-replay + monotonicity), non-tautologically. Peer review (Codex/Gemini) caught + got fixed: a tautological accept_range action (removed), a bogus z3 teeth test (replaced with a real gap-accept discrimination witness), and an over-broad formal_spec=true flip (reverted). Codex follow-up flipped runtime_invariants after adding _check_nonce_batch_runtime_invariants to the live accept path. Codex nonce-batch differential follow-up flipped differential_tests after adding a typed Rust batch shadow and tests/runtime/test_nonce_batch_conformance.py; that suite found and fixed two reject-precedence drift bugs in the Rust parser before the flip. STILL OPEN for a full clear: formal_spec (formalize the batch WRAPPER, or a machine-checked batch==sorted-fold refinement), proof_artifact (also CI-gate the ESSO run and complete review/refinement), running_impl (authority-path entry exists at dex_engine.py:132 + autotrader_live.py:2628; pending the spec/proof binding). nonces is now 3/7 (authority_mode + runtime_invariants + differential_tests)."
    },
    "replay_guard": {
      "running_impl": {"ref": "src/core/replay_guard.py (CBC-core single-transition reference)", "verified": false, "note": "NOT on the live authority path: no non-test/non-tooling importer of src.core.replay_guard exists under src/ (grep-confirmed). The LIVE spot replay authority is src/state/nonces.py (the `nonces` surface; dex_engine.py:132, require_all_nonces=True). replay_guard.py is the proof-carrying CBC-core form, bound to nonces.py on the SINGLE-TRANSITION domain by tests/runtime/test_replay_guard_nonce_refinement_binding.py (11/11). DECIDED (Codex review, Option A): reclassified as evidence-only, attached to the nonces surface (claim_role=evidence_only) and EXCLUDED from the claim AND (see structural_finding)."},
      "formal_spec": {"ref": "src/tau_specs/recommended/nonce_replay_guard_v1.tau; formal/tau/contracts/nonce_replay_guard_v1.contract.json", "verified": false, "note": "Re-bound off the gitignored experiments/cbc_core_v0/ ref (absent from the clean clone) to the TRACKED Tau spec + contract. Specifies strict-sequential single-use (nonce==last+1, per-sender). Not yet cross-checked as the authoritative spec bound to the running impl."},
      "proof_artifact": {"ref": "rust-runtime/crates/zenodex-runtime-core/src/replay_guard.rs (4 #[kani::proof]; CI runtime-kani.yml)", "verified": false, "note": "Re-bound off the gitignored experiments ref to the TRACKED Rust Kani harnesses (classify_is_total / accept_iff_successor / reject_codes_exact / covers_are_reachable; non-vacuous). Chain to running code: Kani-Rust = replay_guard.py (differential, runtime-shadow.yml) = nonces.py on the SINGLE-TRANSITION slice only (binding test). Multi-sender batch atomicity is UNPROVEN."},
      "differential_tests": {"ref": "tests/runtime/test_replay_guard_conformance.py", "verified": false, "note": "Genuine CI-gated (runtime-shadow.yml) Python<->Rust differential, 3 passed; binds replay_guard.py = Rust(Kani) incl 5-code reject parity + 400-tx fuzz. Withheld from verified:true because it pins the CBC-core REFERENCE form, not the live authority (nonces.py). Pre-positioned evidence; would flip only if replay_guard were wired in as authority (not planned — reclassified evidence-only for nonces)."},
      "runtime_invariants": {"ref": "tests/runtime/test_replay_guard_semantic_invariants.py; tests/runtime/test_replay_guard_nonce_refinement_binding.py", "verified": false, "note": "6/6 semantic invariants (isolation/monotonic/anti-replay/reject-no-op) + 11/11 single-transition binding pass on the CBC-core form. NOT enforced as a runtime check in the live authority path; binds to nonces.py single-transition only."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py", "verified": false, "note": "Honestly declared python_authority, fail-closed (no silent fallback; 91 selector/profile tests pass). Withheld from verified:true pending the surface scope decision — a green cell on an off-authority-path surface reads as progress toward an unreachable clear."},
      "open_gaps_closed": false,
      "claim_role": "evidence_only",
      "attached_to": "nonces",
      "structural_finding": "Phase 1 (PR claude/prod-promotion-phase1-replay-guard): replay_guard.py is OFF the live authority path; src/state/nonces.py (the `nonces` surface) is the running replay authority. Added tests/runtime/test_replay_guard_nonce_refinement_binding.py binding replay_guard.py = nonces.py on the SINGLE-TRANSITION domain (11/11, CI-gated). Two open gaps block any clear: (1) CONSENSUS reject-precedence seam: admit checks sender-before-nonce, the batch checks nonce-before-sender, so a double-fault input reports a different reject reason (both still reject + no-op). Reject precedence is part of the consensus contract; a canonical order must be declared. (2) cross-sender batch ATOMICITY (all-or-nothing rollback) is the live authority's distinctive behavior and has NO proof (the chain reaches only the single-transition slice; strictly-more-conservative != proven). DECISION (Codex review): Option A — replay_guard is reclassified as evidence-only (claim_role=evidence_only, attached_to=nonces) and EXCLUDED from the claim AND; its Kani + differential + binding proofs are carried as evidence for the nonces surface. Zero evidence columns flip on replay_guard."
    }
  }
}
