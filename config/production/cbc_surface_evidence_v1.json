{
  "schema": "zenodex/cbc-surface-evidence/v1",
  "scope_id": "spot_dex",
  "claim_scope": ["cpmm_swap", "balances", "state_root", "nonces"],
  "claim_scope_note": "The authority surfaces ANDed into production_security_claim. The gate fails closed if this does not match the registry's non-evidence_only rows (so a real surface cannot be silently dropped by mismarking it evidence_only). Rows with claim_role=evidence_only (e.g. replay_guard) are retained as proof-carriers attached to an authority surface but excluded from the claim.",
  "note": "HONEST work-tracker for the spot-DEX production-claim scope (Phase 0 of docs/PRODUCTION_PROMOTION_PLAN.md). Every column is verified:false until its artifact is GENUINELY checked and bound to the running implementation — refs below are CANDIDATE artifacts, not cleared evidence. The gate (tools/gate_cbc_matrix_closure.py) computes the per-surface and scope claims from this file; it fails closed while any column is unverified. Flip a column to verified:true ONLY when a replayable check proves it clears, per the per-surface promotion checklist. Do not hand-set verified:true.",
  "surfaces": {
    "cpmm_swap": {
      "running_impl": {"ref": "src/core/cpmm.py", "verified": false, "note": "impl identified; needs documented authority-path entry + spec binding"},
      "formal_spec": {"ref": "src/tau_specs/cpmm_v1.tau", "verified": false, "note": "Tau spec + src/kernels/dex/cpmm_output_amount_v2.yaml exist; not yet cross-checked as the authoritative spec"},
      "proof_artifact": {"ref": "lean-mathlib/Proofs/CPMMInvariants.lean; src/kernels/dex/cpmm_output_amount_v2.synth.json", "verified": false, "note": "Lean theorems + Z3/CVC5 receipt exist but are NOT mechanically bound to the running cpmm.py (proven_refinement open)"},
      "differential_tests": {"ref": "tests/runtime/test_cpmm_settlement_conformance.py", "verified": false, "note": "candidate; needs running-impl vs verified-spec differential on recorded blocks"},
      "runtime_invariants": {"ref": "src/tau_specs/cpmm_v1.tau", "verified": false, "note": "needs enforced pre/post invariant checks in the authority path"},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml", "verified": false, "note": "default python_authority; authority_mode field not yet declared/flipped"},
      "open_gaps_closed": false
    },
    "balances": {
      "running_impl": {"ref": "src/state/balances.py (BalanceTable; LIVE store via state.balances in dex_engine.py + state_root.py); src/core/balance_kernel.py (CBC-core credit/transfer reference)", "verified": false, "note": "balances.py BalanceTable IS the live balance store (used as state.balances throughout the engine + state_root) — genuinely on the authority path (unlike replay_guard.py). balance_kernel.py is the proof-carrying CBC-core form (no non-test importer under src/), bound to the live BalanceTable by tests/runtime/test_balance_kernel_live_binding.py. CANDIDATE to clear (the live impl is identified + on-path); left false pending the dual review's call on running_impl-without-a-bound-formal-spec."},
      "formal_spec": {"ref": "rust-runtime/crates/zenodex-runtime-core/src/balance_kernel.rs (#[kani::proof] contracts)", "verified": false, "note": "Rebound off the gitignored experiments/cbc_core_v0/ ref. There is no standalone formal-spec DOC for balances; the machine-checked Kani harness contracts (settle_transfer conserves/moves-exact + reject precedence; settle_credit totality/mints-or-overflows) serve as the spec-as-proof. WITHHELD: a spec-vs-impl artifact distinct from the proof is not yet authored."},
      "proof_artifact": {"ref": "rust-runtime/crates/zenodex-runtime-core/src/balance_kernel.rs (7 Kani, CI runtime-kani.yml --lib); tests/runtime/test_balance_kernel_conformance.py (CI runtime-shadow.yml); tests/runtime/test_balance_kernel_live_binding.py", "verified": false, "note": "STRONG, CI-GATED chain (unlike nonces): runtime-kani.yml runs kani --lib on zenodex-runtime-core, gating balance_kernel.rs's 7 harnesses incl settle_transfer_conserves_and_moves_exact (supply conservation) + non-vacuity covers. Chain to the LIVE store: Kani(Rust) = balance_kernel.py (conformance differential, CI runtime-shadow.yml) = live BalanceTable (test_balance_kernel_live_binding.py: identical (pubkey,asset)->amount maps + conservation on the live store, on the shared domain; balance_kernel is strictly MORE conservative on overflow>MAX_BALANCE + non-canonical format, pinned). All three links CI-gated. CANDIDATE TO CLEAR — left false for the dual review to confirm the chain genuinely reaches the live authority (the live transitions are settlement.py calling BalanceTable.add/subtract; the keystone binds the store ops, not settlement's own validation)."},
      "differential_tests": {"ref": "tests/runtime/test_balance_kernel_conformance.py (Python<->Rust, CI runtime-shadow.yml)", "verified": false, "note": "Genuine cross-implementation differential (balance_kernel.py vs the compiled Rust balance_kernel), reject-code parity + fuzz, CI-gated by runtime-shadow.yml (triggers on balance_kernel.py + balances.py). CANDIDATE TO CLEAR (same kind as the replay_guard differential) — left false pending review."},
      "runtime_invariants": {"ref": "sum_balances_before(asset) == sum_balances_after(asset); tests/runtime/test_balance_kernel_semantic_invariants.py; tests/runtime/test_balance_kernel_live_binding.py", "verified": false, "note": "Supply conservation + per-key isolation + reject-is-no-op are CI-gated property tests on balance_kernel.py (semantic_invariants) AND shown to hold on the LIVE BalanceTable (live_binding). Still NOT an enforced runtime assertion inside the settlement/BalanceTable path — left false until such a check exists or the property tests are promoted to that standard."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py; tests/runtime/test_balance_kernel_live_path.py", "verified": true, "note": "CLEARED (Phase 3): python_authority is the source default (authority.py DEFAULT_MODE), fail-closed on disagreement/Rust-error/timeout/malformed (no silent fallback); production-strict.yaml per_surface {} / promoted_surfaces []. tests/runtime/test_balance_kernel_live_path.py proves credit/transfer route to pure-Python under PYTHON_AUTHORITY and FAIL CLOSED (AuthorityError) on injected disagreement or missing Rust binary; BalanceTable itself has zero Rust coupling. CI-gated by runtime-shadow.yml. The declared mode honestly equals what runs."},
      "open_gaps_closed": false,
      "review_question": "Phase 3 (branch claude/prod-promotion-phase3-balances). Balances is higher-yield than nonces: the live store (balances.py) is on the authority path AND balance_kernel.rs Kani is CI-gated (runtime-kani.yml --lib). Authored tests/runtime/test_balance_kernel_live_binding.py (9 tests) binding balance_kernel.py to the live BalanceTable (identical maps + conservation, divergences pinned). Flipped only authority_mode (nonces precedent). FOR THE DUAL REVIEW: does the CI-gated chain Kani(Rust) = balance_kernel.py (conformance) = live BalanceTable (keystone) justify flipping proof_artifact and/or differential_tests and/or running_impl, or do the documented divergences (balance_kernel more conservative; settlement's own validation not bound) keep them false? Do NOT flip pre-review."
    },
    "state_root": {
      "running_impl": {"ref": "src/state/state_root.py", "verified": false, "note": "impl identified"},
      "formal_spec": {"ref": "src/kernels/dex/ (state-root field-list spec, TBD)", "verified": false, "note": "ESSO spec for the full field list not yet authored"},
      "proof_artifact": {"ref": "(TBD: Z3/CVC5 receipt for determinism + completeness)", "verified": false, "note": "no proof artifact yet"},
      "differential_tests": {"ref": "tests/runtime/test_state_root_vectors.py", "verified": false, "note": "vectors exist; needs spec differential"},
      "runtime_invariants": {"ref": "state_root(post_state) == receipt.post_state_root", "verified": false, "note": "needs enforced runtime check"},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml", "verified": false, "note": "default python_authority"},
      "open_gaps_closed": false,
      "blocking_gap": "D-CANON-002: state root omits fee_accumulator/oracle/vault/perps; must be closed before this surface can clear."
    },
    "nonces": {
      "running_impl": {"ref": "src/state/nonces.py (validate_and_apply_intent_nonce_batch; LIVE authority via dex_engine.py:132, require_all_nonces=True)", "verified": false, "note": "Identified as the live spot replay authority. Pending: bind the proof chain (below) to the multi-sender BATCH wrapper, not just the single-transition slice."},
      "formal_spec": {"ref": "src/kernels/dex/nonce_batch_sequencing_v1.yaml (ESSO; z3+cvc5 VERIFIED); src/tau_specs/recommended/nonce_replay_guard_v1.tau", "verified": false, "note": "STRONG EVIDENCE, not yet a cleared surface spec (peer review: an initial verified:true flip was an overclaim and was reverted). The tracked ESSO model nonce_batch_sequencing_v1.yaml formally specifies + machine-proves (validate + verify-multi z3,cvc5 VERIFIED, both solvers agree, deterministic) the per-sender SINGLE-TRANSITION strict-sequential policy: inv_contiguous (last_nonce==accepted_count, preserved ONLY because accept_one's guard is the strict successor n==last+1 -> gapless prefix => anti-replay + strict-sequential + no-gap + no-double-apply; non-tautological, with a z3 gap-accept teeth witness in tests/runtime/test_nonce_esso_model.py) and inv_monotone_step. WITHHELD because it proves the per-element core, NOT the batch WRAPPER (the validator's sorted==[last+1..last+k] range check, cross-sender atomicity, in-batch dedup) — that remains test-pinned (tests/runtime/test_nonces_batch_binding.py), not formally specified. REMAINING to clear: a formal spec of the batch wrapper (or a machine-checked refinement that batch == sorted-fold of this core)."},
      "proof_artifact": {"ref": "src/kernels/dex/nonce_batch_sequencing_v1.yaml (verify-multi z3+cvc5 VERIFIED); tests/runtime/test_nonce_esso_model.py (repro harness); rust-runtime/crates/zenodex-runtime-core/src/replay_guard.rs (4 Kani, CI runtime-kani.yml)", "verified": false, "note": "STRONG but NOT YET CI-gated. The ESSO proof (4 inductive queries: init + accept_one + reject_stale + reject_gap, z3+cvc5 agree, VERIFIED) + z3 non-vacuity witness + the single-transition Kani are all genuine. WITHHELD because: (1) the proof is run by external/ESSO, which is GITIGNORED and NOT executed by any CI workflow (test_nonce_esso_model.py SKIPS the live verify-multi when ESSO is absent), so per the same bar applied to the gitignored experiments refs it is not reproducibly enforced; (2) the ESSO model proves the per-sender sequencing CORE, not the batch WRAPPER (atomicity/dedup/order remain test-pinned). REMAINING to clear: add a CI workflow that clones ESSO + installs z3/cvc5 and runs verify-multi on this model (assert VERIFIED), and formalize the batch wrapper."},
      "differential_tests": {"ref": "tests/runtime/test_replay_guard_conformance.py (Python<->Rust, CI runtime-shadow.yml)", "verified": false, "note": "Differential is on the replay_guard reference form, transitively related to nonces.py via the single-transition binding. A nonces.py-direct BATCH differential is still needed."},
      "runtime_invariants": {"ref": "nonce_after(sender) == nonce_before(sender) + 1; tests/runtime/test_nonces_batch_binding.py; tests/runtime/test_replay_guard_semantic_invariants.py", "verified": false, "note": "Phase-2 tests/runtime/test_nonces_batch_binding.py pins reject-is-no-op (input table never mutated), per-sender isolation, cross-sender atomicity, strict-sequential, and u32/positive bounds as CI-gated PROPERTY tests on the LIVE authority. Still NOT an enforced runtime assertion inside nonces.py's batch path — runtime_invariants remains open until either such a check exists or these property tests are promoted to that standard."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py", "verified": true, "note": "CLEARED (Phase 2): python_authority is the source default (authority.py DEFAULT_MODE) and is fail-closed on Python/Rust disagreement, Rust error, timeout, or malformed output ('No silent fallback. Unsupported modes raise.'). production-strict.yaml declares per_surface {} / promoted_surfaces []. nonces.py has ZERO Rust/authority coupling (grep-confirmed) so the live batch path cannot be flipped. CI-gated by runtime-shadow.yml (test_authority_selector + deploy-profile tests, 91 passing) which load the real on-disk profile. The declared mode honestly equals what runs."},
      "open_gaps_closed": false,
      "blocking_gaps": "Phase 2 added tests/runtime/test_nonces_batch_binding.py (38 tests, CI-gated by runtime-shadow.yml) which PINS the batch semantics of the live authority and DOWNGRADES both gaps from unproven to tested-but-not-formally-proven: (1) reject-precedence is now CANONICALIZED to the authority's order (nonce-before-sender) and tested (test_canonical_precedence_is_nonce_before_sender); the Phase-1 single-transition binding is correctly scoped to single-fault inputs because the replay_guard reference checks sender-first. (2) cross-sender ATOMICITY, ORDER-INDEPENDENCE, in-batch DEDUP, per-sender ISOLATION, and reject-is-NO-OP are pinned as deterministic property tests, and the multi-sender WRAPPER LAW binds the batch accept+final-state to a per-sender sorted fold of the proven single-transition admit core. PROGRESS: Phase 2b added the ESSO model nonce_batch_sequencing_v1.yaml (z3+cvc5 VERIFIED inductive) that machine-proves the per-sender SINGLE-TRANSITION sequencing core (gapless-prefix/anti-replay + monotonicity), non-tautologically. Peer review (Codex/Gemini) caught + got fixed: a tautological accept_range action (removed), a bogus z3 teeth test (replaced with a real gap-accept discrimination witness), and an over-broad formal_spec=true flip (reverted). NO column flipped by Phase 2b — the ESSO artifact strengthens formal_spec + proof_artifact evidence without clearing either. STILL OPEN for a full clear: formal_spec (formalize the batch WRAPPER, or a machine-checked batch==sorted-fold refinement), proof_artifact (ALSO CI-gate the ESSO run — external/ESSO is gitignored and no workflow runs it), differential_tests (a cross-language batch differential — current suites are same-language bindings), runtime_invariants (an enforced check in nonces.py, or promotion of the property tests to that standard), running_impl (authority-path entry exists at dex_engine.py:132 + autotrader_live.py:2628; pending the spec binding). nonces remains 1/7 (authority_mode only)."
    },
    "replay_guard": {
      "running_impl": {"ref": "src/core/replay_guard.py (CBC-core single-transition reference)", "verified": false, "note": "NOT on the live authority path: no non-test/non-tooling importer of src.core.replay_guard exists under src/ (grep-confirmed). The LIVE spot replay authority is src/state/nonces.py (the `nonces` surface; dex_engine.py:132, require_all_nonces=True). replay_guard.py is the proof-carrying CBC-core form, bound to nonces.py on the SINGLE-TRANSITION domain by tests/runtime/test_replay_guard_nonce_refinement_binding.py (11/11). DECIDED (Codex review, Option A): reclassified as evidence-only, attached to the nonces surface (claim_role=evidence_only) and EXCLUDED from the claim AND (see structural_finding)."},
      "formal_spec": {"ref": "src/tau_specs/recommended/nonce_replay_guard_v1.tau; formal/tau/contracts/nonce_replay_guard_v1.contract.json", "verified": false, "note": "Re-bound off the gitignored experiments/cbc_core_v0/ ref (absent from the clean clone) to the TRACKED Tau spec + contract. Specifies strict-sequential single-use (nonce==last+1, per-sender). Not yet cross-checked as the authoritative spec bound to the running impl."},
      "proof_artifact": {"ref": "rust-runtime/crates/zenodex-runtime-core/src/replay_guard.rs (4 #[kani::proof]; CI runtime-kani.yml)", "verified": false, "note": "Re-bound off the gitignored experiments ref to the TRACKED Rust Kani harnesses (classify_is_total / accept_iff_successor / reject_codes_exact / covers_are_reachable; non-vacuous). Chain to running code: Kani-Rust = replay_guard.py (differential, runtime-shadow.yml) = nonces.py on the SINGLE-TRANSITION slice only (binding test). Multi-sender batch atomicity is UNPROVEN."},
      "differential_tests": {"ref": "tests/runtime/test_replay_guard_conformance.py", "verified": false, "note": "Genuine CI-gated (runtime-shadow.yml) Python<->Rust differential, 3 passed; binds replay_guard.py = Rust(Kani) incl 5-code reject parity + 400-tx fuzz. Withheld from verified:true because it pins the CBC-core REFERENCE form, not the live authority (nonces.py). Pre-positioned evidence; would flip only if replay_guard were wired in as authority (not planned — reclassified evidence-only for nonces)."},
      "runtime_invariants": {"ref": "tests/runtime/test_replay_guard_semantic_invariants.py; tests/runtime/test_replay_guard_nonce_refinement_binding.py", "verified": false, "note": "6/6 semantic invariants (isolation/monotonic/anti-replay/reject-no-op) + 11/11 single-transition binding pass on the CBC-core form. NOT enforced as a runtime check in the live authority path; binds to nonces.py single-transition only."},
      "authority_mode": {"ref": "config/deploy/production-strict.yaml; src/runtime/authority.py", "verified": false, "note": "Honestly declared python_authority, fail-closed (no silent fallback; 91 selector/profile tests pass). Withheld from verified:true pending the surface scope decision — a green cell on an off-authority-path surface reads as progress toward an unreachable clear."},
      "open_gaps_closed": false,
      "claim_role": "evidence_only",
      "attached_to": "nonces",
      "structural_finding": "Phase 1 (PR claude/prod-promotion-phase1-replay-guard): replay_guard.py is OFF the live authority path; src/state/nonces.py (the `nonces` surface) is the running replay authority. Added tests/runtime/test_replay_guard_nonce_refinement_binding.py binding replay_guard.py = nonces.py on the SINGLE-TRANSITION domain (11/11, CI-gated). Two open gaps block any clear: (1) CONSENSUS reject-precedence seam: admit checks sender-before-nonce, the batch checks nonce-before-sender, so a double-fault input reports a different reject reason (both still reject + no-op). Reject precedence is part of the consensus contract; a canonical order must be declared. (2) cross-sender batch ATOMICITY (all-or-nothing rollback) is the live authority's distinctive behavior and has NO proof (the chain reaches only the single-transition slice; strictly-more-conservative != proven). DECISION (Codex review): Option A — replay_guard is reclassified as evidence-only (claim_role=evidence_only, attached_to=nonces) and EXCLUDED from the claim AND; its Kani + differential + binding proofs are carried as evidence for the nonces surface. Zero evidence columns flip on replay_guard."
    }
  }
}
