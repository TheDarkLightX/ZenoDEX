# Minimal runtime dependencies for the production integration layer.

# Cryptographic operations (intent/tx signing and verification)
py-ecc>=6.1.0  # BLS12-381 signatures (same as tau-testnet)

# AES-GCM encryption for SSS backup envelopes and COSE signature verification.
# Minimum fixed release for CVE-2026-69247.
cryptography>=50.0.0

# CBOR/COSE parsing for AWS Nitro Enclave attestation documents
cbor2>=5.6.0

# Type hints used by runtime modules
typing-extensions>=4.8.0

# Canonical JSON encoding for hashing/signing
canonicaljson>=1.6.0

# Runtime model/config loading
pyyaml>=6.0
